# RADIANT Findings Log

**Status:** Active — created 2026-07-31 (owner-ratified two-tier tracking strategy).

The sub-CU tier of Rule 21's intake. A finding lands here when it fails all four CU
intake tests (results-affecting, owner-gated, blocking, workflow-visible) but must
still be written down before its discovering PR merges — no silent debt, no ceremony
for trivia.

**Format** — one appended line per finding, newest last:

```
- YYYY-MM-DD `path:line` — one-sentence symptom. (origin: task or CU/commit)
```

**Exits** (exactly three):
1. **Promoted** — someone decides to work it: mint a CU citing this line, strike the line in the promoting commit.
2. **Struck** — fixed in passing: delete the line in the same PR that fixes it.
3. **Expired** — a quarterly sweep deletes lines nobody promoted. Deletion is legitimate: the intake test already ruled these non-critical.

Lines carry no IDs and need no reservation push (Rule 21 tier 2). Keep each line
self-contained — a reader must understand the finding without opening anything else.

---

## Log

- ~~2026-07-31 `src/radiant/gui/widgets/matplotlib_canvas.py` — one shiboken `RuntimeError: FigureCanvasQTAgg already deleted` printed to stderr per full GUI run (repro: `test_source_instrument.py::TestReflectiveTab::test_solar_rows_do_not_open_an_editor`). **Not** the queued-idle-draw race CU-313 assumed: this matplotlib's `_draw_idle` already guards with `_isdeleted(self)`, and clearing `_draw_pending` at both discard and `destroyed` was measured not to silence it (2026-07-31 attempt, discarded as redundant). The traceback shows the C++ object valid at the `_isdeleted` check, gone by `self.update()` after the Agg render inside the same `_draw_idle` call — the deletion lands *mid-draw*, mechanism unidentified. matplotlib's own `except: print_exc` is what prints it; the draw is on a dying canvas so nothing is lost. Next attempt should start by finding what destroys the canvas during an in-progress synchronous draw. (origin: demoted from CU-313; sharpened 2026-07-31)~~ **Promoted 2026-09-12** to CU-358 (bounded investigation charter).
- ~~2026-07-31 `src/radiant/atmosphere/interpolated.py` — the shipped up-looking ladder is rendered at a single solar zenith (30°), so every query at any other `solar_zenith_rad` emits the CU-167 "is IGNORED" `UserWarning` twice per run, training operators to ignore it; de-duplicate per run, or retire when a multi-solar-zenith family ships (MODTRAN batch 2). (origin: demoted from CU-307)~~ **Promoted 2026-09-12 (October sweep review, owner-ratified)** — to the MODTRAN run queue: rows S1–S15 authored (solar-zenith fan 0/60/75° × the five K rungs; with the existing 30° column, a rectangular (target_altitude, solar_zenith) up-looking grid); README run-queue updated. Lands when the owner next runs MODTRAN.
- ~~2026-07-31 `src/radiant/geometry/stage.py` — a config with a target below its own terrain (`site_elevation_m = 900`, `target_altitude_m = 0`) passes GeometryStage silently and raises only inside `cn2()` when an HV profile is selected; a GeometryStage consistency check (both endpoints ≥ site elevation) would catch it at the right stage with the right framing (Rule 16). (origin: demoted from CU-303)~~ **Struck 2026-09-11 (October sweep)** — GeometryStage now validates the topology-mapped pairing (target≥site down-looking, sensor≥site up-looking, both level) with an actionable error; the far endpoint stays unconstrained (valley-to-peak is physical). Doc + CHANGELOG updated.
- ~~2026-07-31 `scenarios/01…1.3/outputs/` (fig2, fig3), `scenarios/03…3.2/outputs/` (3 figs), `scenarios/03…3.4/outputs/` (3 figs) — 8 committed figures are not byte-reproducible from their own unmodified generators (suspected hash-dependent series/legend ordering, same family as the fixed CU-292); diagnose the ordering, fix at source, regenerate once with cause. (origin: demoted from CU-291)~~ **Struck 2026-09-08 (early quarterly sweep)** — refuted: the 2026-08-31/09-01 measurements and the 2026-09-07 verification found scenario figures pixel-deterministic (double runs byte-identical on 1.3, 3.4, and the 26-figure `0acbf9a8` sweep); the 8 figures were stale numbers, since regenerated.
- ~~2026-07-31 `CLAUDE.md` (gate battery) — `scenarios/` sits outside both the lint and pytest gate scopes and nothing records whether that is a decision or an oversight (822 ruff findings there, all style, all parse clean); decide and write one sentence next to the gate battery — if it ever joins, it needs per-file-ignores for the narrative-script idiom first. (origin: demoted from CU-278)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — scenarios/ joined the ruff lint+format gates (correctness classes enforced, incl. 43 Py3.12-only f-strings fixed — these CRASHED on 3.11; style residue grandfathered per-tree for the chartered sweep to retire); pytest exclusion recorded as deliberate — runners are verified by the §5.3 sweep. CLAUDE.md battery updated.
- ~~2026-07-31 `src/radiant/gui/widgets/scripting_console.py:624` — every figure plotted from the scripting console is retained in `self._figure_windows` forever, even after the analyst closes its window, so hours-long sessions grow the live widget tree unbounded and the theme toggle re-polishes dead figures; decide closed-figure semantics (gone vs reopenable), then prune on close (`WA_DeleteOnClose` + `destroyed` hook) or cap the set. (origin: demoted from CU-285)~~ **Struck 2026-09-11 (October sweep)** — semantics decided: closed means gone (re-plot to see it again); windows now WA_DeleteOnClose with a destroyed-hook prune of the tracked list, so the widget tree no longer grows per plot and theme polish never walks dead canvases.
- ~~2026-07-31 `src/radiant/geometry/stage.py` (no attitude output) — platform/sensor attitude (`observer_{yaw,pitch,roll}_rad`) has no stage owner; ADR-0006 §4's "GeometryStage output vs viewer-local input" decision stays open with no v1 consumer. Decide at the first task that actually needs sensor/platform attitude. (origin: demoted from CU-122 half (ii); half (i), the broken `dev_tools/geometry_gui_v2` shell, was already resolved by deletion 2026-07-19)~~ **Struck 2026-09-12 (October sweep review)** — investigated: no `observer_{yaw,pitch,roll}` parameter exists in any `_schema.py` (the GUI viewer hits are its own camera state), and ADR-0006 decision 4 explicitly records the deferral: attitude was deleted with the flat-Earth dataclasses and "returns only when a consumer exists." The premise (an unowned dangling surface) is contradicted — there is no surface.
- ~~2026-08-01 `modtran/README.md` ("What's not here") — the NPZ-library coverage sentence still counts "25 of the 39 runs", batch-0 arithmetic that predates the 88-row (now 125-row) matrix; it is explicitly scoped to the archived plan §7.2 so it was left as-is when the rest of the README was refreshed for batch 2. Reword against the current matrix at the next README touch. (origin: P0 batch-2 deck authoring, branch modtran/batch2-decks)~~ **Struck 2026-09-11 (October sweep)** — sentence now points at MANIFEST.md + the run matrix as the source of truth instead of baking in a count that drifts.
- ~~2026-08-01 `docs/architecture/RADIANT_Atmosphere.md:797` — the three-way ANGLE-agreement claim says it covers "every delivered row of the 88-row matrix"; still true (the matrix is 125 rows but only 88 are delivered), and it goes stale the moment batch 2 (rows M1–Q8) is delivered. Re-verify and reword at batch-2 ingestion. (origin: P0 batch-2 deck authoring, branch modtran/batch2-decks)~~ **Struck 2026-09-11 (October sweep)** — resolved by the doc rework: the contract doc's claim is now count-free ("every delivered slant row", evidence delegated to parity §1.1/§1.2), and parity §1.1 verifiably covers every delivered row including batch 2.
- 2026-08-01 `scenarios/*/inputs/*.gui.yaml` (38 baselines) — no shipped golden or GUI baseline sets `atmosphere.model: interpolated` (even 8.1/8.2, the interpolation demonstrations, emit `model: simple`; their scripts use the separate `scripts/synth_modtran/family_interpolate.py` tool), so the whole interpolated backend has zero golden coverage — the results-affecting CU-306 change moved not one committed baseline. Consider one interpolated-backend baseline. (origin: CU-306 golden sweep)
- ~~2026-08-01 `scenarios/01…1.6/walkthrough.md`, `scenarios/10…10.2/walkthrough.md` — two more §5.3 refreshes that earlier results-affecting work skipped, found while refreshing the detection-range rows in the same tables: 1.6's signal/SNR were 24 345 e⁻ / 30.5 against the 21 229 e⁻ / 25.8 the runner produces today, and 10.2's sweep table was off in the fourth digit throughout (τ 0.6356 vs 0.6357, noise 735.5 vs 735.6 e⁻ RMS at 25 km). Both corrected in passing by the CU-263 PR with explicit attribution; the pattern — a results-affecting PR refreshing only the rows it moved — is what leaves these behind. (origin: CU-263 §5.3 sweep)~~ **Struck 2026-09-12 (October sweep review)** — the instances were corrected in passing by CU-263; the pattern is now addressed by the §5.3 enumerate-from-runners rule.
- ~~2026-08-01 `docs/architecture/RADIANT_Scripting_API.md:1334` — the "not a method" table still says "Detection range is not a computed metric; sweep `source.target.range_m` against an SNR threshold", which Gap 77 obsoleted in 2026-07: `detection_range_m` has been a `result.metrics` key since then. Reword to point at the metrics key like the RER/GSD half of the same row. (origin: CU-263 doc sweep)~~ **Struck 2026-09-11 (October sweep)** — table cell corrected: `detection_range_m` has been a registry metric since Gap 77/GF-15 (point-source regime only), and the cell now says so instead of recommending a manual range sweep.
- ~~2026-08-01 `scripts/update_golden.py` — `RADIANT_Testing_Validation.md` §5.3 claims the update reason is appended to `_provenance.notes`, but the script has no `--reason` flag and never writes notes; every refresh hand-edits it (CU-267's did). Doc-claims-more-than-code gap; note the process-machinery moratorium before tooling it. (origin: CU-267)~~ **Struck 2026-09-11 (October sweep)** — `--reason` is now mandatory and appended to `_provenance.notes` (duplicate of this line logged 2026-08-29 struck together).
- ~~2026-08-02 `tests/integration/fixtures/modtran/` — the run matrix's `test_fixture + shipped_library` destination predates the owner's 2026-08-02 decision to track `modtran/real_runs/` in git (`c2587fd`). Copying the five promoted batch-2 tape7s into a committed fixture directory would now duplicate 21 MB of already-committed source data with two sources of truth (Rules 26/27), so the promotion shipped as a MANIFEST + anchor tests reading the canonical path instead. Either restate the matrix's `destination` vocabulary or record this as the convention. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — convention recorded in `modtran/README.md`: `test_fixture` = anchored in place at the canonical tracked path, never copied; CSV vocabulary left untouched (tooling reads it by column).
- ~~2026-08-02 `tests/integration/test_modtran_manifest.py:26`, `tests/integration/test_uplooking_horizontal_anchors.py:60`, `tests/integration/test_modtran_real_runs.py` — the `skipif(not _REAL_RUNS.exists())` guards and their "gitignored until the fixture subset is committed — plan §7.1" reasons are stale since `c2587fd` tracked the run set. They now never skip, so nothing is wrong; the reason strings mislead a reader into thinking the data is absent in CI. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)~~ **Struck 2026-09-11 (October sweep)** — all three modules' guard reasons and docstrings now say the set is tracked (since `c2587fd`) and the skipif covers only an incomplete checkout.
- ~~2026-08-02 `scripts/build_atmosphere_library.py` — the module still describes `modtran/real_runs/` as "staged, gitignored" in two places and `docs/architecture/RADIANT_Atmosphere.md` §3.2 said the same; corrected in the builder's generator line and the MANIFEST, but the phrase recurs across `modtran/README.md`, `scenarios/**/gaps.md`, and several test docstrings. One sweep when someone is next in those files. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)~~ **Struck 2026-09-11 (October sweep)** — the surviving "gitignored" claim in the builder updated (tracked since `c2587fd`); the modtran README's remaining use correctly describes the regenerable decks, not the run set; the §3.2 doc claim was already fixed at P5b.
- ~~2026-08-02 `docs/plans/modtran_run_matrix.csv` rows O1/O2 — marked `shipped_library`, but nadir-only: they have no zenith column, so they cannot join the rectangular `(sensor_altitude_m, path_zenith_rad)` upwelling grid the other three O rows form. Shipped under `validation/` (data, not interpolation nodes) instead. Adding them to `midlat_summer_sensor_ladder` would have re-baselined every existing off-node sensor-altitude query, which is out of scope for an ingestion. If a 1/5 km off-nadir upwelling family is wanted, two more decks (1 km and 5 km at 48.2°/60°) close the grid. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)~~ **Struck 2026-09-12 (October sweep review)** — the O1/O2 exception (nadir-only, shipped as validation data, not grid nodes) is now recorded in the README's destination-vocabulary paragraph.
- ~~2026-08-02 `src/radiant/api/tests/test_config_io.py:150` — FIXED IN PASSING. `test_elements_change_results_physically` asserted `signal_e − nearfield_e` falls when a 0.97·0.96·0.90 element train replaces the example's `transmission_scalar: 0.70`. Both halves were wrong: `signal_e` is the scene integral alone (`nearfield_e` is published beside it and joins the well downstream), so the subtraction removed a term that was never added; and 0.83808 > 0.70, so the scene signal must *rise*. The inequality passed only because the spurious constant happened to outweigh the +19.7 % gain on the then-smaller signal, and CU-224's +53.5 % signal expired the coincidence. Replaced with a pin on the throughput ratio itself. (origin: CU-224 P5a §5.3 sweep, branch atmo/cu-224-lpath-thermal)~~ **Struck 2026-09-08 (early quarterly sweep)** — record-only: the fix landed in the same PR that wrote the line.
- ~~2026-08-02 `tests/integration/test_full_system.py::TestSweeps` — the two SNR-monotonicity sweeps ran into `_base_params`' 2e6 e- full well once CU-224 raised the 8 km MWIR scene's flux, flattening SNR at ≈ √FWC on the top rungs. Widened the well to 1e8 e- inside those two tests so the sweep stays in the photon-limited regime the assertion is about. The broader point: several shipped fixtures sit close enough to saturation that any results-affecting increase trips them, and the failure reads as a monotonicity violation rather than as "the well filled" — a `well_status` assertion in these trend tests would name it. (origin: CU-224 P5a §5.3 sweep, branch atmo/cu-224-lpath-thermal)~~ **Struck 2026-09-11 (October sweep)** — the two widened-well trend sweeps now assert `well_status == 'ok'` per rung through a shared metric helper, so a future flux-raising landing fails as "the well filled at rung X" instead of a baffling monotonicity violation.
- ~~2026-08-02 `src/radiant/atmosphere/interpolation_coverage.py:507` — `check_interpolation_coverage`'s rule-2 remedy still names `recommended_axes(...)`, the axes-string-only heuristic. For an off-vertical up-looking scene it therefore says "Set atmosphere.interpolation_axes = 'target_altitude_m'", which selects the vertical-only ladder and is refused at evaluate — the same mis-recommendation CU-322 fixed on the `Sensor` seam. Not reachable as a wall any more (the GUI now shows the pre-validated gap advisory when nothing serves the scene, and the picker proposes the correct family beside the refusal when something does), but the message text is still one step behind the selector. Fixing it needs the LOS, which this module cannot build (Rule 11 forbids `atmosphere` importing `geometry`), so it is a design question, not a text edit. Candidate CU-322 checklist item. (origin: CU-322, branch gui/cu-322-interpolated-switch)~~ **Struck 2026-09-11 (October sweep)** — `recommended_axes` is now θ-aware for up-looking scenes (vertical within the 1e-6 rad hard slack, under either caller's zenith convention, else None), so the rule-2 remedy no longer steers an off-vertical up scene into the vertical-only ladder's refusal; the error falls back to the directory/simple remedies.
- ~~2026-08-02 `src/radiant/gui/widgets/atmosphere_family_picker.py:349` — the picker's coverage line quotes `ShippedFamily.coverage` verbatim for the pre-selected family even on a wholly-vacuum LEO→GEO path (scenario 10.4), where no backend is consulted at all: it reads "covers ground sensor (0 km) looking up at targets 0-20 km" beside a 500 km → GEO scene. The API now flags the case (`AtmosphereFamilySuggestion.vacuum_path`, used by the generated `gui_workflow.md` notes); the picker does not read it yet. (origin: CU-322, branch gui/cu-322-interpolated-switch)~~ **Struck 2026-09-11 (October sweep)** — the picker now reads the full `atmosphere_family_suggestion()` and, on `vacuum_path`, replaces the coverage sentence with a vacuum note (τ = 1, L_path = 0, no backend consulted; family named only to fill the parameter).
- ~~2026-08-02 `scenarios/tools/gui_baselines.py` (CU-319 count) — the CU-319 entry names eight `AttributeError` baselines plus 4.3; a full `emit_gui_yaml.py` run on the unmodified base commit fails **thirteen**: 6.3 and 7.2 (`no attribute 'sensor'`) and 7.3 and 7.4 (`no attribute 'config'`) break the same way and were never listed. All thirteen are fixed in this PR (the CU-319 verification is 38/38 `[ok]`), so this is a record that the entry's enumeration was short, not open work. (origin: CU-319, branch fix/cu-319-318)~~ **Struck 2026-09-08 (early quarterly sweep)** — record-only: the short enumeration was corrected in the CU-319 PR itself.
- ~~2026-08-02 `modtran/synthetic/` (`.gitignore:73`) vs `scenarios/08…8.2/scripts/` — scenario 8.2's runner dies with `FileNotFoundError: … modtran/synthetic/C5.synthetic.tp7` on any tree where `scripts/generate_synthetic_tape7.py` has not been run, because the synthetic decks are gitignored by design while `modtran/real_runs/` is now tracked. 8.2 is the only shipped runner with no real-run fallback, so it is the only one that cannot run from a fresh clone; scenario 1.1's walkthrough meanwhile advertises the synthetic set as the fallback that keeps scenarios "runnable from a bare clone", which is true only after the generator is run. Either commit the handful of decks the scenarios need, or say so in 8.2's docs. (origin: CU-317 scenario-walkthrough sweep)~~ **Struck 2026-09-08 (early quarterly sweep)** — discharged by the 2026-09-07 quick-wins fix: 8.2's runner now raises an actionable SystemExit naming `scripts/generate_synthetic_tape7.py`, and its walkthrough carries a Reproducing note.
- ~~2026-08-02 `src/radiant/optics/` EE_box (CU-188 scope) — CU-188's CHANGELOG entry scopes the cell-area-overlap EE_box re-weighting to "point-source and sub-pixel" regimes, but the *reported* `EE(1x1)` metric moved by −14.2 % on EXTENDED scenes too (2.3, 6.3; also 8.3's vacuum rungs), where EE_box ≡ 1 and no signal changes. Harmless — only the diagnostic moved, not the radiometry — but the scope statement reads narrower than the change's actual reach, which made three scenario refreshes attribute it with a written caveat rather than cleanly. Widen the entry's scope wording. (origin: CU-317 scenario-walkthrough sweep)~~ **Struck 2026-09-12 (October sweep review)** — the (unreleased) entry's Effect clause widened: extended-scene radiometry unchanged, but the EE(1x1) diagnostic moves there too.
- ~~2026-08-02 `scenarios/06…6.4/scripts/run_synthetic_scene.py`, `scenarios/10…10.1/scripts/`, `10.3/scripts/`, `10.4/scripts/` — four runners print hardcoded narrative strings that their own computed output now contradicts: 6.4 prints "contrast SNR runs 59–548" while computing 49.3–475.5; 10.3 prints a "STILL ZERO" banner and an underflow explanation immediately after computing a sky background of 3.2774 W/m²/sr/µm, and still predicts the model is "~8× too opaque in the VIS" after its own check passes; 10.1 §6 attributes a detection-range crossover to a sky-thinning mechanism its own (now flat) table refutes; 10.4 §12 says the 180 K bench self-emission "is NOT zero and is left in the budget" while the chain warns it is identically zero. The walkthroughs were corrected by the CU-317 sweep, but these strings will re-stale the docs at the next refresh. Runner-side fix. (origin: CU-317 scenario-walkthrough sweep)~~ **Struck 2026-09-12 (chartered scenario sweep, phase 1)** — 6.4's SNR range and 10.1's crossover conclusion now DERIVE from the rows they describe (10.1's crossover has in fact returned — 0.916–1.041 crossing near 30° — under the accumulated atmosphere landings, proving the derive-don't-hardcode point in both directions); 10.3's STILL-ZERO banner is value-conditional (currently NONZERO, 3.879 W/m²/sr/µm, with the CU-254 explanation), its FAIL diagnosis rewritten to the current CU-337 attribution story, and its MODTRAN-anchor block updated to the delivered batch-2 reality; 10.4's claim was already fixed by Gap 127. All three runners re-run clean.
- ~~2026-08-02 `tests/integration/test_uplooking_horizontal_anchors.py` — the pinned characterization values are a pre-CU-267 vintage and have drifted toward the loose end of their own tolerance bands (K-ladder MWIR τ ratios now 1.295/1.300/1.252/1.166/1.093 against pinned 1.299/1.306/1.259/1.173/1.100 in a ±0.04 band; level-arm LWIR 0.953/0.883/0.753 against 0.954/0.885/0.756 in ±0.03). Still green, but the margin is being spent silently, and the 10.2 walkthrough's "agrees to three figures" claim had to be softened because of it. Re-pin at the next touch. (origin: CU-317 scenario-walkthrough sweep)~~ **Struck 2026-09-11 (October sweep)** — K-ladder repinned to freshly measured values (worst drift: K5 LWIR radiance 1.033 → 0.979, 0.006 from its band edge), repin recorded in the characterization docstring; MODTRAN reference columns untouched.
- 2026-08-02 `scenarios/*/gui_workflow.md` (2.5 Step 6, 6.2 lines 28–32, 5.2:59, 3.1) — four GUI mock-ups whose *design premise*, not merely their numbers, is falsified by current results: 2.5 specifies a stacked-bar panel around background_shot/signal_shot "swapping dominance" and a labelled BLIP region that no longer exists (signal_shot now runs 75.2 %→99.9 % of variance across the sweep); 6.2 states the now-reversed "transmittance residuals don't track SNR residuals" finding as a design requirement; 5.2:59's mock warning string says "8 µm fails MTF, EE, and SNR" though that candidate passes SNR and always did; 3.1 quotes NIIRS/GSD at 42° off-nadir, a point on no sweep grid. Redesign, not renumbering — deliberately left by the CU-317 sweep, which refreshed numbers only. (origin: CU-317 scenario-walkthrough sweep)
- ~~2026-08-02 `scenarios/06…6.4/walkthrough.md` — the scenario's central methodological premise says filled-pixel target signals are range-independent, but its own runner shows S_tgt falling 8.853e5 → 6.352e5 e⁻ from 10 → 200 km as the atmospheric path grows. Equally false at the doc's own vintage, so it is not sweep drift; the CU-317 pass declined to rewrite a scenario's core narrative claim unilaterally. Needs a physics ruling, then a rewrite. (origin: CU-317 scenario-walkthrough sweep)~~ **Struck 2026-09-12 (chartered scenario sweep)** — premise corrected with the physics ruling it needed: filled-pixel signals are GEOMETRICALLY range-independent (no 1/R²) but fall through path transmittance (measured −31 % over 10→200 km in the scenario's own table); the analytic-dilution sweep is now stated as an approximation with that measured error envelope, not an identity.
- ~~2026-08-02 `scenarios/10…10.3/scripts/`, block 10 `default_off_metrics` — two small unattributables found by the sweep: 10.3 raises two full-well saturation `UserWarning`s (4.627e5 e⁻ against a 4e5 e⁻ well) from its GEO daylight reflective probe that the runner never surfaces in stdout, and `diffraction_limit_target_plane_m` joined the scene-class `default_off_metrics` list for all four block-10 scenarios (10 → 11 entries) with no Results-affecting CHANGELOG entry covering it. Neither changes a reported number; both were refreshed in the docs as observed rather than attributed. (origin: CU-317 scenario-walkthrough sweep)~~ **Struck 2026-09-12 (chartered scenario sweep)** — split: the default_off CHANGELOG omission is repaired with a retroactive Unreleased entry; the saturation half re-logged below as a sharper line (still live on the composed tree).
- ~~2026-08-02 `examples/scripts/compare_configs.py` — `examples/` is outside the four trees the ruff gate governs (`src/ tests/ scripts/ dev_tools/`, widened by CU-252/CU-270), and `ruff format --check examples/` already reports this file as drifted. Shipped example scripts are read as reference code and are edited by tasks that touch physics (this one was), so the same drift the two earlier widenings closed is accumulating in a fifth tree. One-line gate question, not a defect. (origin: CU-315 example-caveat update, branch fix/cu-315-323)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — examples/ joined the ruff lint+format gates at full rules (ships in the wheel since Gap 126); its two accumulated findings fixed in the joining commit.
- ~~2026-08-02 `tests/integration/test_modtran_tape7_import.py`, `test_modtran_real_runs.py`, `test_segment_modtran_anchors.py`, `test_batch2_fixture_anchors.py` — no committed integration anchor exercises a tabulated- or MODTRAN-served τ on a chain grid that differs from the file's: every one of them either reads the parser output directly (`_read`/`_components`), queries on the file's own grid (a bit-identical no-op), or anchors a radiance/irradiance (which stays linear). CU-316 changed the τ resample convention for both backends — a results-affecting change — and the §5.3 sweep therefore moved **zero** committed anchors; the movement is pinned only by the new Level-0 tests. Same class of gap the CU-306 closure logged for `atmosphere.model: interpolated`. One off-node-grid anchor per backend would close it. (origin: CU-316, branch atmo/cu-316-logtau-backends)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — `TestOffGridTauAnchor` pins A1's band-mean τ on a 200-point off-grid chain (0.5155036, rel 1e-4; a linear-τ regression moves it 150× the tolerance), closing the class CU-316 exposed.
- ~~2026-08-02 `src/radiant/atmosphere/interpolated.py:968` vs `log_tau_resample.py` — the two log-τ paths disagree on over-unity τ: the interpolated backend clips its result to [0, 1] after `exp()` (and its constructor clips stored τ to [TAU_FLOOR, 1.0] before the log), so a mis-scaled family file is silently snapped to a plausible column, while the shared `resample_transmittance` floors only at TAU_FLOOR so the same defect in a tabulated/MODTRAN file still fails loud in `AtmosphericQuantities.__post_init__` (Rule 17). CU-316 deliberately did not touch the interpolated behaviour; the asymmetry is now the only place the three backends' τ conventions differ. (origin: CU-316, branch atmo/cu-316-logtau-backends)~~ **Struck 2026-09-11 (October sweep)** — the disagreement is resolved in the resampler's direction (whose pass-through of over-unity τ is a pinned contract precisely so mis-scale stays visible): the interpolated constructor now REJECTS τ > 1 + 1e-9 at load with an actionable error naming the point, instead of silently clipping a mis-scaled family into a plausible column; float dust still loads; the post-exp query clip remains for interpolation dust only.
- 2026-08-02 `scenarios/01…1.1/walkthrough.md`, `scenarios/06…6.2/walkthrough.md` — CU-316's log-τ resample DID move committed scenario prose, contradicting its own closure note that "zero committed goldens/baselines move": 1.1's MODTRAN-6 column reads τ 0.4319 → 0.4277 / SNR 917.36 → 916.18 / range 2239.7 → 2227.3 km, and 6.2's SNR-MODTRAN column moves 0.5–0.7 % on every profile. Both are tape7-import arms, which is exactly the backend CU-316 changed. The closure's claim was true of machine baselines and goldens (none select those backends) and false of runner-generated walkthrough tables, which no §5.3 sweep covers by default — the CU-317 class of gap, one week later. Refreshed in passing by the CU-321 sweep with CU-316 attribution. (origin: CU-321, branch atmo/cu-321-height-teff)
- ~~2026-08-03 `src/radiant/atmosphere/` (θ_s-stripped `T1Thermal` sky coupling) — the limitation `RADIANT_Atmosphere.md` §4.2g records as "found 2026-07-26, not yet repaired" has NO registry entry anywhere — only a characterization test and the doc note; it predates the two-tier intake. Now also item 12 of `docs/validation/atmosphere_modtran_parity.md`'s limitations register. Decide its tier at next triage. (origin: Atmosphere Documentation Plan P-D1)~~ **Promoted 2026-09-12** to CU-356.
- ~~2026-08-03 `src/radiant/gui/stage_views.py` / metric registry — "Diffraction limit (at target, legacy key)" renders as a visible duplicate row; "Sampling regime 0 code" and "NIIRS (extrapolated) 1 0/1 flag" leak raw registry vocabulary to the analyst-facing default screen. Decode table or suppression at display time. (origin: GUI design critique, metric cards)~~ **Struck 2026-09-11 (October sweep)** — display-time decode table added (`_CODED_VALUE_DISPLAY`: sampling regime and NIIRS-extrapolated render as words) and the CU-231 alias suppressed from the cards (`_SUPPRESSED_DISPLAY_KEYS`); badge and card share one finite-value renderer so the pinned rail agrees (CU-326 pattern).
- ~~2026-08-03 `docs/architecture/RADIANT_Atmosphere.md` §3.1 + §4.2g — two measured records still live in the slimmed architecture doc because `atmosphere_modtran_parity.md` has no corresponding table: the A2–A6 profile cross-validation claim (≤ ±0.012 band-mean τ) and the CU-254 `background_e` measurement (1.94207e5/2.14046e5/2.21479e5 e⁻). Re-home both into the parity doc at its next touch, then trim here. (origin: Atmosphere Documentation Plan P-D3)~~ **Struck 2026-09-12 (October sweep review)** — both re-homed: the A2–A6 ≤ ±0.012 cross-validation record into parity §2.6, the CU-254 background_e table into parity §2.7; the contract doc keeps one-line pointers per the four-document reading rule.
- ~~2026-08-03 `mkdocs.yml` — the new Validation nav section navigates only `atmosphere_modtran_parity.md`; the five pre-existing `docs/validation/` documents were never in the nav and remain out. Decide whether they join at the next docs-infrastructure touch. (origin: Atmosphere Documentation Plan P-D3)~~ **Struck 2026-09-11 (October sweep)** — decided joined: all six remaining validation docs added to the Validation nav section.
- ~~2026-08-03 `src/radiant/atmosphere/family_suitability.py:715` — with two SST fans rendered at different lower endpoints, a scene at a third elevation reports whichever fan comes first in `BUNDLED_FAMILIES` (the 0 m one), not the nearer one: `select_atmosphere_family` breaks equal-gate ties by catalogue order, never by how close the miss is. Advisory quality only, and no shipped scenario sits at a third elevation. (origin: M9-M13 / P7-P8 follow-on ingestion)~~ **Struck 2026-09-11 (October sweep)** — equal-gate ties now break on miss distance within the same gap kind (`_closer_miss`/`_miss_distance`), so a third-elevation scene names the nearer SST fan; cross-kind ties keep catalogue precedence (no common unit to compare on).
- ~~2026-08-03 (process) — the batch-4 merge (`474cd0c8`) briefly took `main` red on one GUI test: the battery invocation piped `pytest -q` through `tail`, so the pipeline's exit status was tail's and the failure was masked until after the push. Fixed forward within minutes (`49b9fc17` — the test carried a pre-CU-329 expectation; the merged code was correct). Agent practice, not tooling: battery invocations must `set -o pipefail` or capture pytest's status directly before merging. (origin: gui/param-panel-finish merge)~~ **Struck 2026-09-08 (early quarterly sweep)** — executed: the same hole (pytest piped through `tail` masking the exit status) nearly passed an unverified suite again 2026-09-08; the battery-invocation practice sentence is now in CLAUDE.md's gate section, added in this sweep.
- ~~2026-08-29 `src/radiant/atmosphere/simple.py:313` (`_ESKY_DIFFUSIVITY_D = 1.1`) — the CU-155 fitted flux-diffusivity exponent is measurably worse than the downwelling ladder's own `sec 48.2 deg = 1.4966` on the nine-rung P block: RMS |ln(model/pi*L_MODTRAN)| 2.0776 -> 1.9233 over 9 rungs x 2 bands, holding `z_em` fixed. D was fit against H2/H4 alone before P1-P8 existed, so the ladder that would have constrained it postdates the fit. Swapping it is a one-constant, results-affecting change to `E_sky_thermal` (every reflected-sky term) and needs an owner ruling — it was outside CU-324 item 1's authorisation, which gated adoption on the *layered* form. Full 2x2 in `docs/validation/atmosphere_modtran_parity.md` §2.14(a). (origin: CU-324 item 1, branch atmo/cu-324-emission-placement)~~ **Struck 2026-08-29** — owner-ratified and swapped on branch `atmo/cu-324-d-swap`: `_ESKY_DIFFUSIVITY_D` is now derived as `sec 48.2 deg = 1.50030` (the line's `1.4966` was a transcription slip; `sec 48.09 deg`). Composite RMS 2.0776 -> 1.9233, tropospheric-only 0.4167 -> 0.3087.
- ~~2026-08-29 `src/radiant/atmosphere/simple.py:222` (`_GasRegion(8.00, 10.00, floor_od=0.2751)`) — the CU-161 region table has no 9.6 um ozone region: one flat 2 um-wide region spans both the clean 8-9.4 um window and the O3 nu2 band, so the model carries no identifiable ozone opacity to place. This is the hard blocker under CU-324 item 2 — the placement fix is worth a measured 34 % on the 9.4-9.9 um feature but its one free parameter (the O3 share of the floor OD) is unidentifiable until the region is split. A tau-side (CU-161-class) recalibration, results-affecting on tau itself, not an emission-placement task. (origin: CU-324 item 2, branch atmo/cu-324-emission-placement)~~ **Promoted to CU-330** (owner ruling 2026-08-29); split landed the same day on branch `atmo/cu-330-o3-region`.
- ~~2026-08-29 `tests/integration/test_batch2_atmosphere_families.py:134` (`test_every_batch2_row_was_delivered_or_is_a_recorded_gap`) — the completeness check is scoped to block letters M-Q via `_BATCH2_BLOCKS`, so a run-matrix block authored under any other letter is invisible to it: the new R block (CU-324 item 3) needed its own delivery tripwire in `test_emission_placement_cu324.py` rather than joining the existing one. Fine today (two tests, two scopes), but a third future block would repeat the pattern; a matrix-wide `FUTURE RUN` check would subsume both. (origin: CU-324 item 3, branch atmo/cu-324-emission-placement)~~ **Struck 2026-09-11 (October sweep)** — roster tripwire added: any delivered post-L block letter missing from `_BATCH2_BLOCKS` now fails the suite, forcing the roster and completeness coverage to grow with new blocks (R1–R3 keep their own CU-324 delivery tripwire).
- ~~2026-08-29 `docs/validation/landsat_oli2_source_data.md` — compiled offline: every value classed "published (recalled)" (aperture 135 mm, focal length 886 mm, pitch 36/18 µm, t_int 3.6 ms, and the L8 measured-SNR column ±15%) needs a live-web verification pass against [Knight 2014]/[Morfitt 2015] before the measured column is treated as authoritative; promote or correct classes in place. (origin: scenario 9.4 authoring)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — verification pass run via Wayback: focal length 886 mm verified verbatim against the archived Knight 2014 full text (snapshot URL + retrieval date recorded in the doc); the remaining recalled rows stay recalled-class with the attempt and its blockers (MDPI 403, no numerics on NASA design pages) recorded in the doc header.
- ~~2026-08-29 `src/radiant/gui/stage_views.py:507` / `api/plot.py:plot_coating_spectra` — coating plot renders on the chain grid with a fixed [0,1] axis; coating dispersion invisible. (origin: scenario 9.4 GUI review)~~ **Promoted to Gap 116** (2026-08-29).
- ~~2026-08-29 `tests/integration/test_modtran_real_runs.py::test_esky_thermal_simple_vs_modtran_characterization` — the H2/H4 `E_sky_thermal` parity compares a thermal-only model quantity against `Tape7Reader.to_radiant_units()`'s path radiance, which is thermal **+ scattered**; at these ground-rooted daylight decks that inflates the 3-5 um reference ~18 % (LWIR unaffected to 3e-4), so the pinned MWIR ratios read ~18 % low by construction. Harmless as a regression pin (both sides are stable), misleading as a physics statement. The new nine-rung ladder test in the same module uses the thermal column via `_modtran_esky_thermal_only`; fold the H2/H4 pair onto it at the next touch. (origin: CU-324 item 1 D-swap, branch atmo/cu-324-d-swap)~~ **Struck 2026-09-12 (October sweep review)** — re-based on `_modtran_esky_thermal_only`: MWIR references drop 34 % (H2) / 13 % (H4) of scattered contamination, the pinned ratios rise to their like-for-like values (0.869 → 1.167, 0.928 → 1.046), and the envelope re-measured; LWIR moves ≤ 1e-3.
- ~~2026-08-29 `scripts/update_golden.py` — `RADIANT_Testing_Validation.md` §5.3 says a golden update's reason "is also appended to `_provenance.notes`", but the script only preserves the existing notes and rewrites `last_updated`; the note is a hand edit the protocol never names as one. Every past refresh did it by hand and the convention (newest first, ` | `-separated) is undocumented. Either the script grows a `--reason` flag or §5.3 says "append by hand". (origin: CU-324 item 1 D-swap, branch atmo/cu-324-d-swap)~~ **Struck 2026-09-11 (October sweep)** — `--reason` is now mandatory and appended to `_provenance.notes` (duplicate of this line logged 2026-08-29 struck together).
- ~~2026-08-29 `scenarios/01_sarah_systems_engineer/1.6_mwir_point_source_sda/walkthrough.md` — the results table was stale against `main` BEFORE this PR: it read SNR 17.67 / detection range 1,254.7 km, while the unmodified runner on the pre-CU-324 tree gave 20.35 / 1,347.8 km (signal 20,933 e- unchanged). Some landing between 2026-08-02 and 2026-08-29 moved it and nobody re-ran the scenario; the CU-324 sweep found it in passing and refreshed the numbers, but did not bisect which landing owns the 15 % jump. This is the CU-317 failure mode recurring — runner-generated walkthrough tables are covered by no automatic gate. (origin: CU-324 item 1 D-swap, branch atmo/cu-324-d-swap)~~ **Promoted to the scenario-drift CU-334 and struck 2026-08-29** — bisected to CU-321 (`6cf6eaa9`, 2026-08-03); walkthrough attribution and the missing **Results-affecting** CHANGELOG entry landed on branch `analysis/cu-331-16-bisect`.
- ~~2026-08-29 (process) `scenarios/*/*/inputs/*.gui.expected.json` — a §5.3 sweep that enumerates moved scenes through the GUI baselines cannot see the nine scenario folders that have no baseline (1.6, 2.4, 4.2, 6.5, 8.3, 9.1-9.4): 38 baselines cover 47 folders. That is exactly how CU-321's otherwise thorough refresh missed 1.6, the only moved scene without one. Enumerate from the runners (as the CU-324 D-swap sweep did, all 43) or note the gap in the protocol. (origin: CU-334 bisect, branch analysis/cu-331-16-bisect)~~ **Struck 2026-09-12 (October sweep review)** — protocol rule added to §5.3: sweeps enumerate from the runners, never the baselines.
- ~~2026-08-29 `scripts/fit_simple_atmosphere_gas_bands.py:126` — the CU-161 generator has not run since ADR-0011: it builds `LineOfSightGeometry(h_tgt=0.0, theta_o=0.0, ...)` with no `h_sensor`, so `SimpleAtmosphere.evaluate` raises `ParameterBoundsError` and the script dies before printing a single row. A generated artifact whose generator does not execute is unverifiable (Rule 26). (origin: CU-330, branch atmo/cu-330-o3-region)~~ **Struck 2026-08-29** — fixed in the same PR: `h_sensor=100_000.0`, matching the `geometry.sensor_altitude_m` the script already sets.
- ~~2026-08-29 `scripts/fit_simple_atmosphere_gas_bands.py::_model_nonwater_od` — the generator is not idempotent: `floor_add = max(0, OD0 - nonwater)` where `nonwater` is evaluated from the *current* model, which since CU-161 already contains the calibrated `floor_od`. Re-running it therefore returns `floor_add ~ 0` for every region and would silently zero the table it generated. (origin: CU-330, branch atmo/cu-330-o3-region)~~ **Struck 2026-08-29** — fixed in the same PR by zeroing the regions' `floor_od` for the duration of the non-water evaluation, which restores the original convention exactly; the script now reproduces the shipped 8-10 um row (0.2751 / 0.0877 / 1.268) bit-for-bit.
- ~~2026-08-29 `src/radiant/atmosphere/simple.py::_CALIBRATED_GAS_REGIONS` (VIS/NIR/SWIR rows) vs `d169febd` (CU-253) — **candidate CU, results-affecting; needs an owner ruling.** With the generator repaired (two lines above) it reproduces the shipped LWIR rows exactly but NOT the VIS/NIR ones: 0.45-0.70 um refits to `floor_od` 0.1597 against the shipped 0.0000, and 0.70-1.30 um to 0.0517 against 0.0000. Cause: those rows were calibrated in 2026-07-17 when the Rayleigh optical depth was ~8x too large, so the measured floor was already met by Rayleigh+aerosol and `floor_add` clamped to zero; CU-253 later cut Rayleigh 8x and nobody re-ran the fit. Measured consequence at the A1 anchor: model band-OD 0.293 against MODTRAN's 0.453 over 0.45-0.70 um, i.e. the model is ~17 % too transmissive in the VIS. Smaller drifts in 1.50-1.75 (0.0219 vs 0.0133), 2.05-2.40 (0.0749 vs 0.0725) and 2.40-3.10 (0.7444 vs 0.7434). CU-330 deliberately re-ran only the 8-10 um row it was authorised to touch. (origin: CU-330, branch atmo/cu-330-o3-region)~~ **Promoted to CU-335** (owner ruling 2026-08-30); the re-fit landed 2026-08-30 on branch `atmo/cu-335-visnir-refit`.
- ~~2026-08-29 `tests/integration/test_uplooking_horizontal_anchors.py::test_level_arm_vs_the_full_horizontal_grid` — the docstring claims "above 3 km the LWIR arm holds within ~±3 % out to 25 km range", but its own pinned table contradicted that BEFORE this PR: the 5 km / 25 km cell (L13) was 0.893, i.e. −10.7 %, and 3 km / 25 km (L8) was 0.951, −4.9 %. The CU-330 repin moved those to 0.885 and 0.945 and left the claim alone rather than rewrite a characterization sentence it did not author. The numbers are right and the prose is optimistic; tighten the sentence to what the table says at the next touch. (origin: CU-330, branch atmo/cu-330-o3-region)~~ **Struck 2026-09-11 (October sweep)** — docstring claim tightened to the table's own numbers (~2 % to 10 km range, ~6–12 % at 25 km, worst L13 = 0.885).
- ~~2026-08-30 `scripts/fit_simple_atmosphere_gas_bands.py::_model_nonwater_od` — **candidate CU, results-affecting; needs an owner ruling.** The generator mixes two spectral grids: `floor_add` is the ladder's band OD (tape7 grid, uniform in wavenumber) minus a non-water reference measured on `np.linspace(0.30, 14.29, 3000)` (uniform in wavelength). Where the spectrum is steep the two band means differ and the gap lands in the floor, always toward an over-large one: measured +0.022 OD at 0.45-0.70 um, +0.011 at 0.70-1.30 um, <= 0.0004 beyond 1.3 um. It is why the CU-335 re-fit improved 0.45-0.70 um band-mean tau parity 5.3x but moved 0.70-1.30 um the wrong way (RMS |ln r| 0.0312 -> 0.0402). Fixing it means evaluating the reference on the ladder's own grid, which changes every floor in the table and is therefore a new calibration, not a re-run. (origin: CU-335, branch atmo/cu-335-visnir-refit)~~ **Struck 2026-09-08 (early quarterly sweep)** — discharged by CU-336 (RESOLVED 2026-09-01): the non-water reference now evaluates on the tape7 grid; the measured offsets landed exactly as this line predicted.
- ~~2026-08-30 `src/radiant/atmosphere/simple.py::_CALIBRATED_GAS_REGIONS` (0.45-0.70 um row) — **candidate CU, results-affecting; needs an owner ruling.** CU-335's re-fit puts 0.1597 optical depths on this row's *well-mixed gas* floor, but real 0.45-0.70 um gas chemistry supplies only ~0.03 (O3 Chappuis; the O2 B/A bands are narrow). The remaining ~0.13 is an aerosol-model deficit wearing a gas label. The band total is right against MODTRAN (within 4 % at the anchor) and the attribution is not, so any product separating gas from aerosol - or scored against a source assuming a cleaner aerosol than the configured one - still disagrees: scenario 10.3's astronomical-extinction anchor flips PASS -> FAIL on exactly this (0.127 -> 0.282 mag/airmass against a published 0.12-0.20 good-site band). Recalibrating the VIS aerosol is the fix; it supersedes the old "~2x high at rural-23" statement, whose sign CU-253 had already reversed. (origin: CU-335, branch atmo/cu-335-visnir-refit)~~ **Struck 2026-09-08 (early quarterly sweep)** — promoted: this finding became CU-337 (owner-approved 2026-09-01, scheduled behind the MODTRAN-work deferral).
- ~~2026-08-30 (process) `scenarios/*/*/outputs/*.png` — a results-affecting sweep regenerates every scenario figure, but the PNGs are not byte-reproducible (matplotlib rendering differs run to run even where the numbers are bit-identical: measured on 4.3, 4.4 and 4.5, all byte-identical in stdout, all with changed PNG bytes). CU-330 and CU-335 both reverted the figure churn rather than commit it, so committed figures now lag the numbers beside them by two results-affecting landings. Either the figures are regenerate-on-demand (Rule 26 (b) does not apply) and should be gitignored, or the sweep protocol should say to commit them. (origin: CU-335, branch atmo/cu-335-visnir-refit)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — premise contradicted by the 08-31 double-run pixel verification; ruling: figures stay committed (Rule 26(b)) and sweeps regenerate moved scenarios' figures same-machine same-PR (§5.3); the CU-330/CU-335 lag is repaid by the chartered sweep.
- ~~2026-08-30 (process) `scenarios/tools/emit_gui_yaml.py` + `tests/integration/test_gui_baselines.py:14` — the documented §5.3 baseline-refresh command is `python scenarios/tools/emit_gui_yaml.py`, with no `PYTHONPATH`. Run that way from a **worktree**, the editable-install `.pth` resolves `radiant` to the primary checkout, so the refresh silently regenerates every baseline against a *different* library than the branch under test — here it rewrote 15 baselines from another agent's in-progress main tree instead of the 6 this change actually moves, and the corruption is invisible because the emitter reports `[ ok ]` for all of them. `PYTHONPATH=./src` fixes it (CLAUDE.md's editable-install caveat already says so for scenario runners; the refresh command is the same hazard and is not covered). Add the prefix to the docstring and to §5.3 at the next touch. (origin: CU-324 item 2, branch atmo/cu-324-o3-emission)~~ **Promoted to CU-338** and fixed 2026-09-01: every `scenarios/tools` entry point now calls `_local_radiant.ensure_local_radiant()`, which prepends the invoking tree's `src` and refuses with an actionable error if `radiant.__file__` still lands outside it; §5.3 drops the `PYTHONPATH` prefix.
- ~~2026-08-30 `src/radiant/atmosphere/emission_temperature.py::_column_fraction` — the exponential branch keeps the pre-existing `total <= 0.0 → skip` fallback, which silently drops a species' optical depth from the layer sum (breaking the telescoping identity) if its column ever underflows over a segment. Unreachable in practice for the shipped scale heights (the smallest, 1.2 km aerosol over a 100 km column, bottoms at `exp(−83) ≈ 8e−37`), and the new layer branch is immune by construction (log-domain weights). Either make the exponential branch log-domain too, or turn the fallback into a Rule-15 raise. (origin: CU-324 item 2, branch atmo/cu-324-o3-emission)~~ **Struck 2026-09-11 (October sweep)** — the exponential branch is now carried in the log domain like the Gaussian branch (relative exponents survive normalisation at any altitude), so the underflow-to-None path is unreachable and no species' opacity can drop from the layer sum; Level 0 test pins the 900–1000 km / H = 1.2 km regime against the analytic shares.
- ~~2026-08-30 `scenarios/06_dr_chen_researcher/6.4_synthetic_scene_generation/walkthrough.md` — the detection-range-sweep table's **Contrast (e-)** column was stale against `main` BEFORE this PR: it read 36 379 e- at 200 km and 5 821 e- at 500 km where the unmodified runner gives 35 963 and 5 754 (the contrast column is bit-identical across CU-330 and CU-324 item 2, so the staleness predates both). Corrected in passing by the CU-324 sweep. Same CU-317/CU-334 pattern — a runner-generated walkthrough column that no automatic gate covers — but on a column two earlier refreshes copied forward instead of re-reading. (origin: CU-324 item 2, branch atmo/cu-324-o3-emission)~~ **Struck 2026-09-08 (early quarterly sweep)** — record-only: corrected in passing by the CU-324 sweep that wrote the line.
- ~~2026-08-30 `scenarios/08_interpolation_demonstrations/8.2_target_altitude_interpolation/scripts/run_target_altitude_interpolation.py` — the runner dies with `FileNotFoundError: modtran/synthetic/C5.synthetic.tp7` on a clean checkout: it needs the generated synthetic tape7 set, which is gitignored and has no build step named in the scenario. Its GUI baseline still refreshes (that path uses the inline builder in `gui_baselines.py`, not the runner), so the scenario is half-reproducible — the numbers move and the walkthrough cannot be re-derived. Either name the generator in the scenario README or commit the fixture subset. (origin: CU-324 item 2 walkthrough sweep, branch atmo/cu-324-o3-emission)~~ **Struck 2026-09-07** — fixed on branch `findings/quick-wins`: the runner now raises an actionable SystemExit naming `python scripts/generate_synthetic_tape7.py` as the prerequisite, and the walkthrough carries a Reproducing note (generator + the GUI-baseline exemption).
- ~~2026-08-31 `src/radiant/atmosphere/simple.py::_region_params` — **caught in the CU-324/CU-335 merge, fixed in the same commit.** The CU-324 landing gave `_region_params` a `regions: tuple = _CALIBRATED_GAS_REGIONS` **default argument**, which Python binds once at class-body evaluation. `scripts/fit_simple_atmosphere_gas_bands.py` isolates its non-water reference by rebinding that module global to zeroed floors — and a default-argument snapshot ignores the rebinding, so the reference re-included the shipped floors and every row refit to ~0: the exact non-idempotency CU-330 had repaired, reintroduced through a new seam and invisible until CU-335's fit-reproduction tests composed with it. The general lesson is not about this function: **any import-time capture of `_CALIBRATED_GAS_REGIONS` (default argument, module-level derived constant, cached-once table) silently defeats the generator's floor-zeroing.** Both seams CU-324 introduced now read the live global at call time and are pinned by `test_ozone_placement.py::test_the_gas_floor_evaluation_reads_the_live_table_not_an_import_snapshot`. A grep for new captures belongs in any future review of that table. (origin: CU-324 item 2 / CU-335 merge, branch atmo/cu-324-o3-emission)~~ **Struck 2026-09-08 (early quarterly sweep)** — record-only: caught and fixed in the same commit; the import-snapshot lesson is pinned by `test_the_gas_floor_evaluation_reads_the_live_table_not_an_import_snapshot`.
- ~~2026-08-31 (process) `scenarios/*/*/walkthrough.md` — **composition residue.** A walkthrough refreshed on a branch is measured against a tree that does not contain its siblings, so when the siblings merge the numbers go stale again at the ~1e-4 level and nobody re-runs. Measured on four scenarios after the CU-335 / CU-324-item-2 merge: 1.3 (MWIR pixel signal, contrast, 1200 K SCNR — and its "every MWIR figure is bit-identical" claim, which was true on the branch and false on `main`), 3.2 (two PWV-row τ digits, baseline signal), 10.2 (six signal rungs, τ@25 km, three detection-range digits, α_eff) — each branch-side attribution paragraph is individually correct and their composition is not. The antidote is one sweep run on the composed tree after the last sibling merges, not per-branch refreshes. (origin: scenarios/composition-residue)~~ **Struck 2026-09-12 (October sweep review)** — protocol rule added to §5.3: results-affecting sweeps run once on the composed tree after the last sibling merges.
- ~~2026-08-31 `scenarios/{01_sarah_systems_engineer/1.3_dual_band_mwir_lwir,03_raj_mission_planner/3.2_weather_sensitivity,03_raj_mission_planner/3.4_off_nadir_agility,10_direction_general/10.2_air_to_air_level_irst}/outputs/*.png` — ten committed figures across these four scenarios still disagree with their runners on `main`: the `scenarios/figure-regen` sweep (`0acbf9a8`) covered 26 figures but not these, i.e. the same four scenarios the composition missed on the text side. Also corrects the 2026-08-30 premise above that scenario PNGs are not byte-reproducible: a double run of 1.3 on one tree is **pixel-identical**, so the churn measured here is moved numbers, not matplotlib nondeterminism. Left uncommitted by this PR (scope was docs); regenerate with the next figure sweep. (origin: scenarios/composition-residue)~~ **Struck 2026-09-07** — discharged: the `0917b32b` figure sweep regenerated the ten figures shortly after this line was written, and a fresh run of all three remaining runners (1.3, 3.2, 10.2) on today's composed `main` reproduces every committed PNG byte-for-byte (branch `findings/figure-sweep`); 3.4's four were already ruled current by the 2026-09-01 correction below.
- ~~2026-08-31 `scenarios/03_raj_mission_planner/3.4_off_nadir_agility/walkthrough.md` ("RADIANT GSD vs. True Off-Nadir GSD") — the table and the prose around it are stale by more than a refresh: they read RADIANT GSD 1.47/1.53/1.61/1.71/1.85/2.04 m at 20-45° with "+9.6 % error at 45 deg", while the runner now prints 1.45/1.50/1.56/1.64/1.74/1.86 m with **+0.0 % at every angle** — RADIANT's GSD metric tracks true cross-track GSD exactly. Predates the CU-335 landing (geometry-flexibility era). Not a number swap: it would retire the section's finding and move the scenario's Gap 33/34 status from OPEN (partial), so it needs an owner-visible disposition rather than an in-passing edit. (origin: scenarios/composition-residue)~~ **Struck 2026-09-01** — owner-ruled and executed on branch `scenarios/ruled-dispositions`: table refreshed, section's finding retired, Gap 33/34 marked CLOSED in the walkthrough and their `Rerun after fix` lines updated in `gaps.md`. Attribution found rather than left open — the stale column is `p·R(η)/f` digit for digit, the θ_o-vs-η misread closed by `65720f0d` (2026-07-12, CU-096/CU-097, ADR-0006 Phase 2), not the geometry-flexibility era this line guessed.
- ~~2026-08-31 `scenarios/03_raj_mission_planner/3.2_weather_sensitivity/walkthrough.md` + `01_sarah_systems_engineer/1.3_dual_band_mwir_lwir/walkthrough.md` — hand-derived prose ratios lag their tables by a whole vintage: 3.2's "SNR moves only ~5.4 % (507.9 → 480.6)" and "signal itself 10.4 % lower (258,362 → 231,395 e⁻)" are pre-CU-324 (runner now gives 508.7 → 481.3 and 259,167 → 232,030; the percentages still round the same), and 1.3's "LWIR's clutter is ~217× MWIR's" is 231.8× on every vintage this document records. Refresh protocols update tables and skip the prose that quotes them. Left for the next touch — the conclusions are unaffected. (origin: scenarios/composition-residue)~~ **Struck 2026-09-07** — fixed on branch `findings/figure-sweep`: 3.2's prose now reads 10.5 % (259,226 → 232,080 e⁻) and ~5.4 % (508.7 → 481.3) from today's runner (the numbers had drifted a second time since this line was written); 1.3's clutter ratio corrected to ~232× (measured 231.8×).
- ~~2026-09-01 `docs/validation/atmosphere_modtran_parity.md` §3 (known-limitations register) — the row numbers are not unique: the table runs 14, 15, 16, 14, 15, because the CU-335 and CU-324 landings each appended a row without checking the tail. Pre-existing before CU-336, which added the 16. Renumbering churns every cross-reference that cites a row by number, so it wants one deliberate pass rather than an in-passing edit. (origin: CU-336, branch fix/cu-336-338)~~ **Struck 2026-09-11 (October sweep)** — the duplicate 14 was the superseded pre-CU-335 row (deleted, Rule 27); the ozone row renumbered 15 → 17 with its row-4 cross-reference updated; register 1–17 now unique.
- ~~2026-09-01 `scenarios/03_raj_mission_planner/3.4_off_nadir_agility/scripts/run_off_nadir_agility.py:584-585`, `:730`, `:751`, `:919` — the runner's *narrative* still asserts the finding the 2026-09-01 owner ruling just retired on the walkthrough side, immediately above its own refuting table: it prints "RADIANT's GSD metric uses nadir formula: GSD = p × H / f / It does NOT account for path_zenith_rad (Gap 33)" and then a +0.0 % error column, lists "RADIANT GSD metric does not account for off-nadir angle (Gap 33)" under Limitations, and bakes "RADIANT GSD (nadir only)" / "NIIRS (RADIANT — nadir GSD only)" into the fig2/fig3 legends. Same family: the walkthrough rows for Gap 35 ("Both GSD axes equal in RADIANT" — the chain now reports 1.86 cross vs 2.63 along at 45°) and Gap 36 ("Must compute externally") also predate their gaps.md CLOSED status. Deliberately left by the ruled disposition, whose scope was docs-only; a runner-text pass would also need to re-rule Gap 35/36 and regenerate two figures (verified pixel-deterministic on this tree). (origin: scenarios/ruled-dispositions)~~ **Struck 2026-09-01** — executed on branch `scenarios/34-runner-prose`: the four runner sites plus the same claim in the module docstring and two inline comments rewritten, Gap 35/36 re-ruled CLOSED in the walkthrough, fig2/fig3 regenerated. Prose-only — all 162 data-bearing stdout lines byte-identical before and after.
- ~~2026-09-01 `scenarios/10_direction_general/10.3_ground_to_space_sst_visible/walkthrough.md` (Anchor 2) — **the aerosol premise of the 2026-09-01 ruling is not in the scenario.** The walkthrough says the scene "runs a 900 m site with **rural 23 km visibility**, whose aerosol alone carries ~0.17 optical depths at 550 nm"; the scenario's own tasking card (`sst_site_and_tasking.xlsx`, "Meteorological Visibility") and `.gui.yaml` both say **100 km** ("Exceptional dry-air night"), where the Koschmieder aerosol carries 0.0223 OD, not 0.17. The 23 km/rural pair is the *MODTRAN fan's* deck setting (runs M9–M13, `docs/plans/modtran_run_matrix.csv` IHAZE=rural VIS=23), so the phrase looks to have leaked from the interpolated-family provenance into the simple-model narrative. Consequence: the anchor's FAIL is not aerosol-reachable — the non-aerosol zenith OD floor is 0.2377 (k_V = 0.258 mag/airmass) against a published good-site top of 0.20, so k_V stays outside the band at *any* visibility, and the measured sweep runs 0.363 (23 km) → 0.306 (50) → 0.288 (80) → 0.282 (100, shipped) → 0.263 (500, schema max). The dominant term is CU-335's 0.1597 gas floor, already logged above as an aerosol deficit wearing a gas label. Recorded, not resolved: needs a re-ruling. (origin: scenarios/ruled-dispositions)~~ **Struck 2026-09-12 (chartered scenario sweep)** — the paragraph now states the scenario's own 100 km/0.022 OD configuration (the 23 km/0.17 OD pair was the MODTRAN fan's deck setting, conflated), the measured any-visibility unreachability (k_V floor 0.258 vs ≤ 0.20), and CU-337 as the only fix — the 2026-09-01 ruling's premise, in the scenario.
- ~~2026-09-01 `scenarios/03_raj_mission_planner/3.4_off_nadir_agility/gaps.md:7-10,71-73` + `gui_workflow.md:40,54,85,171-174` — the scenario's two companion documents still carry Gaps 33/34/35/36 as OPEN ("RADIANT reports nadir GSD even with path_zenith_rad > 0", "Both GSD axes equal in RADIANT", "Must compute externally", and a code block commented "# Compute off-nadir GSD (not available natively — Gap 33)"), all four now CLOSED in `docs/tracking/gaps.md` and in `walkthrough.md`. Left by both 2026-09-01 passes, whose scopes were the walkthrough and the runner; the same scenario now says two different things about the same four gaps depending on which of its four files you open. (origin: scenarios/34-runner-prose)~~ **Struck 2026-09-07** — reconciled on branch `cu340/34-alongtrack-gsd` (the CU-340 PR): all four gap rows in both companion docs now read CLOSED with the ruled notes; the scenario `gaps.md` metrics/noise tables were caught up to the current vintage in the same pass (they still carried 2026-08-02 numbers and a retired `background_shot` term).
- ~~2026-09-07 `scenarios/03_raj_mission_planner/3.4_off_nadir_agility/walkthrough.md:81-87` — the committed walkthrough on `main` carried raw, unresolved merge-conflict markers (`<<<<<<< HEAD` / `>>>>>>> 26245007`) around the nadir NEDT/NIIRS metric rows — the CU-336 merge's resolution was committed half-done, and no gate scans committed text for conflict markers. Second registry-file merge casualty in this scenario family (CU-340's own entry body was dropped twice by merges). A `grep -rn '<<<<<<< HEAD' scenarios/ docs/` on 2026-09-07 found no other instance. (origin: cu340/34-alongtrack-gsd)~~ **Struck 2026-09-07** — fixed in the same PR that found it (conflict resolved to the current-vintage rows, banner phrasing kept); line recorded for the merge-hygiene pattern.
- ~~2026-09-01 (correction) `docs/tracking/Findings_Log.md` 2026-08-31 figure line above — its claim that scenario **3.4**'s committed figures "still disagree with their runners on `main`" does not reproduce: running the unmodified `main` runner on a clean tree regenerates all four of 3.4's PNGs **byte-for-byte identical** to the committed ones, so 3.4's figures were already current and only the two whose legend strings this branch edited actually moved. The line's companion point stands and is reinforced — these figures are pixel-deterministic. Unverified for the other three scenarios it names; someone re-running that check should treat 3.4 as already discharged. (origin: scenarios/34-runner-prose)~~ **Struck 2026-09-08 (early quarterly sweep)** — purpose served: the figure line it corrects was struck 2026-09-07 after full verification (all three remaining scenarios byte-identical too).
- ~~2026-09-02 `src/radiant/api/plot_style.py` `SERIES_LIGHT`/`SERIES_DARK` — a hand-maintained 6-hue subset of `gui/themes/tokens.py` `config_accents` (with `OTHER_*` = slot 7), duplicated hex literals with no mechanical link; `test_plot_style.py` asserts subset-containment only, so a future re-hue of accent slots 0–7 would silently desync the plot palette. Surfaced while growing `config_accents` 8 → 12 (slots 0–7 untouched, so nothing desynced today). (origin: cfgset/phase1-cap12)~~ **Struck 2026-09-11 (October sweep)** — the mirror test now pins per-position slot identity (series positions ↔ accent slots (0,4,5,1,3,2), OTHER ↔ slot 7) instead of subset containment; a re-hue of any mirrored slot now fails CI. The hex duplication itself stays — api/ may not import gui/, so the test is the mechanical link.
- ~~2026-09-02 `src/radiant/gui/widgets/configuration_bar.py` `_TRAILING_ITEMS = 2` — hardcodes the count of pinned trailing layout items; adding a third trailing widget would silently misplace every tab insertion. (origin: cfgset/phase1-cap12)~~ **Struck 2026-09-07** — void: the CU-341 scroll-strip rework (branch `cu341/config-bar-overflow`) moved the tabs into their own layout whose only trailing item is its stretch; the constant is deleted.
- ~~2026-09-02 `src/radiant/gui/widgets/configuration_bar.py:145` `accent_for()` — silently returns slot 0's accent for a name not in the set instead of raising; a quiet fallback in a lookup with no legitimate unknown-name case. (origin: cfgset/phase1-cap12)~~ **Struck 2026-09-07** — fixed on branch `gui/accent-for-raise`: unknown names raise an actionable `KeyError`; the one production caller queries from the same set that populated the bar, so the fallback only ever masked host re-bind ordering bugs.
- ~~2026-09-02 `src/radiant/api/config_set.py` — an element override is run through the io element parser twice per materialization (edit-time validation in `set_element_override`, attach-time normalization in `sensor_for` via `Sensor.set_optical_elements`), so a CSV-backed override re-reads its spectral file on every sensor build; correct but not free, and un-overridden configurations pay nothing. (origin: cfgset/phase2-elements)~~ **Amended 2026-09-02** — the override store it names is gone (§3a-bis); the double-parse itself survives renamed: `set_element_for` validates at edit time and `sensor_for` re-parses at attach time, so a CSV-backed configured entry re-reads its spectral file on every sensor build; rows left shared pay nothing. (origin: cfgset/phase2-elements)
- ~~2026-09-02 `src/radiant/gui/tests/` shiboken `FigureCanvasQTAgg already deleted` traceback — the 2026-07-31 log line (ex-CU-313) records one occurrence per full GUI run; a full run now prints it **twice**. Bisected clean of the Gap-103 element-override diff (`test_source_instrument.py` alone prints 1, the element tests print 0) — a second pre-existing site, not a regression from this branch. (origin: cfgset/phase2-elements)~~ **Promoted 2026-09-12** — folded into CU-358 as worsening-cadence evidence.
- ~~2026-09-02 `src/radiant/gui/widgets/optical_element_editor.py` — element-train commits (shared or override) record no undo command; the documented Phase-9 non-scalar undo limitation now also covers per-configuration overrides, and an orphaned override (console-made, unreachable from the locked GUI) renders as shared-train fallback + API message in the detail pane — a display mitigation, not a fix. (origin: cfgset/phase2-elements)~~ **Struck 2026-09-02** — half void (the override store it names is gone, §3a-bis); the surviving half is restated below.
- ~~2026-09-02 `src/radiant/gui/widgets/optical_element_editor.py` — the "overridden — <configuration>" badge shares the Name cell and clips both the element name and itself at default column width ("band_f overri…"); legible but ugly at 5-element trains with real names. Column sizing or an elided badge with full-text tooltip wants one polish pass. (origin: cfgset/phase2-elements, three-band live demo)~~ **Struck 2026-09-02** — void: the override badge it describes was removed with the override model itself (plan §3a-bis configured element rows, owner live review the same day). A configured row's red "C" is painted after the name by the delegate, so it cannot crowd or clip the Name cell.
- ~~2026-09-02 `src/radiant/gui/widgets/optical_element_editor.py` — no element-document commit records an undo command: not Apply, and not the new *Configure across configurations…* / *Un-configure row…* row actions, which move a row between the shared and the per-configuration store. The documented Phase-9 non-scalar-undo limitation now covers a document-shape change as well as a value change; the way back is the opposite menu action. (origin: cfgset/phase2-elements)~~ **Promoted 2026-09-12** to CU-357 (family head).
- ~~2026-09-02 `src/radiant/api/config_set.py` — the API has no position-preserving structure operation for an element document holding configured rows (no `insert_element` / `remove_element` / `move_element` that shifts the configured positions with the rows), so removing a row *above* a configured one, or reordering across it, is not expressible. The GUI disables those affordances naming the blocking row rather than accepting an edit it cannot write; appending, and removing or reordering below every configured row, are unaffected. Wants an owner call on whether the operations are worth an API pair. (origin: cfgset/phase2-elements)~~ **Promoted 2026-09-12** — folded into CU-357 as its API checklist item.
- ~~2026-09-02 `src/radiant/gui/widgets/optical_element_editor.py` — `QTableWidget.removeRow` emits the selection change while the removed row is still counted but its cell widgets are already destroyed, so the coating-detail slot serialized a half-mutated table and raised `AssertionError` inside the Qt event loop (pane silently dead until the next selection). Pre-existing since the pane landed (Gap 116), reachable by clicking Remove with a row selected.~~ **Struck 2026-09-02** — fixed in passing on `cfgset/phase2-elements`: every structural mutation runs under a `QSignalBlocker` and refreshes explicitly once complete; covered by the remove-a-configured-row test.
- ~~2026-09-03 `src/radiant/cli/main.py` — `python -m radiant.cli.main <args>` silently no-ops (exit 0, no output): no `if __name__ == "__main__"` guard, so the click group is never invoked and the arguments are discarded; a user following the module-invocation form reads clean-exit as success. The installed `radiant` entry point is unaffected. (origin: cfgset/phase3-oli)~~ **Struck 2026-09-07** — fixed on branch `findings/quick-wins`: `__main__` guard added; subprocess tests pin both the group running (`--help` exits 0 with usage) and error propagation (unknown subcommand exits non-zero).
- ~~2026-09-03 `scenarios/09_flagship_missions/9.4_landsat_oli2_snr/data/filter_butcher_block.csv` — orphaned by the all-bands study (no config file references it; generator branch marked historical). Kept deliberately this PR as the D-7-era artifact; Rule 26 favors deleting it and its generator branch on the next 9.4 touch. (origin: cfgset/phase3-oli)~~ **Struck 2026-09-07** — executed on branch `findings/quick-wins`: the CSV and its `gen_oli2_coatings.py` code path deleted (Rule 26/27; generator re-run confirms the remaining 20 files byte-identical); 9.4 walkthrough + scenario gaps.md updated.
- ~~2026-09-06 `docs/tracking/gaps.md:1710` — Summary Table rows end at Gap 111; Gaps 112–120 were minted entry-only (drifted convention: table appears abandoned, entries are authoritative). Either retire the table or backfill at the quarterly sweep. (origin: gap120 minting)~~ **Struck 2026-09-08 (early quarterly sweep)** — executed in this sweep: the Summary Table is retired from `gaps.md` (entries are and were the authoritative record; Rule 25/27).
- ~~2026-09-06 `src/radiant/data/tables/fpa/flir-boson-plus-640.yaml` — uncooled-bolometer presets can hold no NEDT / thermal-time-constant / f-number-scaling data as parameters (no schema home; Gap 101 adjacency): temperature-domain record ships in preset `notes` only, so bolometer SNR/NEDT is not yet drivable from a preset. (origin: gap119/phase1-tranche1)~~ **Promoted 2026-09-12** — folded onto Gap 101 (third instance: the missing detector-class notion, preset side).
- 2026-09-06 `src/radiant/gui/widgets/outputs_readout.py` — advisory strings emitted as scalar stage outputs (e.g. detector `dark_temperature_note`) render as Outputs rows; arguably they belong in the Messages panel instead — the GUI now wraps them, but the outputs-vs-messages routing question stands. (origin: gap119/phase3-gui live review)

*Early quarterly sweep executed 2026-09-08 (scheduled ~2026-10-01, pulled forward at the owner's queue-review go): 11 lines struck with attribution (discharged/refuted/promoted/executed), 12 aged cosmetic lines expired per the Rule 21 quarterly provision (git history is the archive), 56 lines retained — condition-carrying tripwires, candidate-CUs awaiting rulings, and policy questions. The next scheduled sweep is ~2026-12.*
- ~~2026-09-08 `src/radiant/cli/templates.py` — `radiant template list/show/create` serves an INLINE hardcoded template dict (four minimal configs baked into the module), a second template vocabulary that predates and now shadows the six bundled mission templates CU-349 ships; a user comparing `radiant template list` against the welcome screen sees two disjoint sets. Fold the CLI onto `radiant.api.mission_templates` (list/show/create from the bundled six) or rename its offering. (origin: cu349/ship-templates)~~ **Struck 2026-09-12 (October sweep review, owner-ratified)** — the CLI now serves the bundled mission-template store via `mission_templates` discovery; the inline dict deleted (Rule 27), its three uncovered band classes graduated as proper truth-barred templates (welcome catalog 6 → 9, flagged for live review).
- ~~2026-09-08 `docs/tracking/gaps.md` — the gap numbering skips 106: no `## Gap 106` heading exists on any recent `main` (the Active-Imaging work that memory associates with "Gap 106" apparently lives only in `docs/plans/Active_Imaging_Plan.md`, which also never says "Gap 106"). Long-standing hole, surfaced by the 2026-09-08 merge-train continuity check; either the number was consumed by a mint that never landed or the entry was folded without a record. Reconcile at the next gaps.md touch. (origin: merge-train verification)~~ **Struck 2026-09-11 (October sweep)** — reconciled: no Gap 106 ever existed in any registry or history; VOID tombstone entry added so the continuity check has a recorded explanation; number retired.
- ~~2026-09-09 `pyproject.toml` (import-linter) — `lint-imports` was RED on clean `main`: two test-seam chains (readout `test_architecture` → `io.config` → optics factories; calibration `test_detector_handoff` → `detector.stage`, both landed with Gap 117/120 work) broke the stage-independence contract, so every merge since ran against a red gate. (origin: gap127/element-only-emission battery)~~ **Struck same PR** — fixed in passing: two documented test-seam ignore edges added; production-code independence unchanged.
- ~~2026-09-09 `pyproject.toml` (`[[tool.importlinter.contracts]]` "no cross-stage physics imports") — `lint-imports`, a hard merge gate, was RED on `main`: the independence contract's test-seam ignore list lacked `radiant.*.tests.* -> radiant.io.config` (which its sibling "physics stages import only core" contract has, and which reaches optics transitively through `io.element_config`) and the Gap-120 `calibration.tests.test_detector_handoff -> detector.stage` handoff import. Four test modules across source/readout/calibration tripped it; production imports were clean throughout. (origin: cu/well-fill-nearfield gate run)~~ **Struck 2026-09-09** — both ignores added in the same commit; contract battery back to 6 kept / 0 broken.
- ~~2026-09-09 `src/radiant/gui/widgets/optical_element_editor.py:246` — the element table's Diam (m) and →FPA (m) columns are now inert: Gap 128 deleted per-element geometry, so this branch stopped seeding and serializing them and renders them read-only with an explanatory tooltip rather than as editable cells that change nothing. The columns themselves come out with the Transmission-tab redesign (explicitly out of scope for Gap 128; owner ordering 2026-09-09 — physics first, GUI separately with a live review). (origin: gap128/etendue-nearfield)~~ **Struck 2026-09-11 (October sweep)** — the Transmission-tab consolidation (merged 2026-09-11, `1de0a630`) removed both columns; the code comment at the former site records the rationale.
- ~~2026-09-09 `scenarios/07_karen_test_engineer/7.4_cold_stop_sweep/` — the scenario's lab data (35.5–55.8 ke⁻ shuttered background, rising monotonically with cold-stop offset) can no longer be explained by any model knob: a cold stop cannot manufacture background under Gap 128, so a position-dependent background means the stop has stopped being the aperture stop at those offsets. The runner names this as the finding rather than fitting it; a vignetting/partial-stop model would be a new capability if the owner wants one. (origin: gap128/etendue-nearfield)~~ **Promoted 2026-09-12** to Gap 130 (vignetting / partial-stop model).
- ~~2026-09-11 `src/radiant/optics/wavefront.py:175` — `rms_opd_m()` in SCALAR_RMS mode returns `rms_waves × λ_ref` and ignores any folded-in `zernike_coeffs` (the stage's defocus Z4 fold), so the `strehl_marechal` diagnostic understates total WFE whenever scalar RMS is combined with nonzero `defocus_um`; the primary PSF-derived `strehl` is unaffected. Pre-existing, unchanged by CU-355. (origin: cu355/scalar-wfe-zernike)~~ **Struck 2026-09-11 (October sweep)** — `rms_opd_m()` now merges the CU-355 expansion with any folded coefficients per Noll index and RSSes the total, so `strehl_marechal` sees the same budget the pupil does (unobscured expansion; the ε renorm is a few-percent effect the diagnostic doesn't need).
- ~~2026-09-11 `docs/architecture/RADIANT_File_Tree.md:522` — the per-package summary table's `optics/` row (31 source / 22 tests) disagreed with the section heading (30 + 26) before Gap 129 and is not verified by `check_org_rules.py` (which checks only the headings); the table is a second, drifting count. (origin: gap129/pixel-phase)~~ **Struck 2026-09-11 (October sweep)** — full table regenerated from the tree by its own stated convention (had drifted through eleven landings); header now names the convention split vs the check_org_rules headings.
- ~~2026-09-11 `pyproject.toml` (import-linter "no cross-stage physics imports") — `lint-imports`, a hard merge gate, was RED on clean `main`: the Gap 129 merge's `platform.tests.test_stage -> optics.pixel_kernel` test-seam import (rebuilding the pixel-aperture kernel to verify the straddle evaluation) lacked an ignore edge; production platform code imports no optics. (origin: cu354/saturation-warning-label gate run)~~ **Struck same PR** — documented test-seam ignore edge added; contract battery back to 6 kept / 0 broken.
- 2026-09-12 `scenarios/10_direction_general/10.3_ground_to_space_sst_visible/scripts/` — one sub-case still evaluates its nominal point with the well clipped (stderr: pedestal 4.627e5 e⁻ vs full_well 4e5, fill 1.16; both the ReadoutStage and contrast-SNR warnings fire) and the runner surfaces neither in stdout — the warning-free-for-valid-scenarios bar (CU-166 principle). Locate the sub-case, size its well/integration headroom, refresh the affected numbers with attribution. (origin: chartered scenario sweep 2026-09-12, split from the CU-317 unattributables line)
