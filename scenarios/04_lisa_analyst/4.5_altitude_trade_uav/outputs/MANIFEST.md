# Output Manifest

All artifacts below are produced by `scripts/run_uav_altitude_trade.py`
from `inputs/lisa_microbolometer_uav.xlsx` (generated by
`inputs/create_spreadsheet.py`). Regenerate by running the script from the
repo root. The committed figure is referenced by `walkthrough.md`; the
NETD→D* conversion and altitude-trade table are printed to the console.

| Artifact | Kind | Last generating commit |
|---|---|---|
| fig1_altitude_trade.png | figure (committed) | efea031 2026-07-08 |
