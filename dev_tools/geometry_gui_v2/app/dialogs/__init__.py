"""Modal dialogs — About, Settings, Keyboard shortcuts.

Each dialog is one file (Rule 19). Dialogs are intentionally separate
from ``main.py`` so the main window's layout code stays focused on
window assembly, not on per-dialog markup.
"""
