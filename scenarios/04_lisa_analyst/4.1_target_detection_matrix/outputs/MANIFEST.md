# Output Manifest

All artifacts below are produced by `scripts/run_detection_matrix.py` from
`inputs/lisa_target_library.xlsx` + the three `inputs/sensor_*.yaml`
(the workbook generated by `inputs/create_spreadsheet.py`; the sensor YAMLs
are also emitted by it). Regenerate by running the script from the repo
root. Committed figures are referenced by `walkthrough.md`;
`*_results.xlsx` is regenerate-on-demand and not committed (see
`docs/OPERATING_MODEL.md` Rule 26).

| Artifact | Kind | Last generating commit |
|---|---|---|
| fig1_detection_range_matrix.png | figure (committed) | (set on commit) |
| fig2_nadir_scnr_by_target.png | figure (committed) | (set on commit) |
| detection_matrix_results.xlsx | results workbook (not committed) | regenerate on demand |
