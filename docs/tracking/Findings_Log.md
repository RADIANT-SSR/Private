# RADIANT Findings Log

**Status:** Active — created 2026-07-31 (owner-ratified two-tier tracking strategy).

The sub-CU tier of Rule 21's intake. A finding lands here when it fails all four CU
intake tests (results-affecting, owner-gated, blocking, workflow-visible) but must
still be written down before its discovering PR merges — no silent debt, no ceremony
for trivia.

**Format** — one appended line per finding, newest last:

```
- YYYY-MM-DD `path:line` — one-sentence symptom. (origin: task or CU/commit)
```

**Exits** (exactly three):
1. **Promoted** — someone decides to work it: mint a CU citing this line, strike the line in the promoting commit.
2. **Struck** — fixed in passing: delete the line in the same PR that fixes it.
3. **Expired** — a quarterly sweep deletes lines nobody promoted. Deletion is legitimate: the intake test already ruled these non-critical.

Lines carry no IDs and need no reservation push (Rule 21 tier 2). Keep each line
self-contained — a reader must understand the finding without opening anything else.

---

## Log

- 2026-07-31 `src/radiant/gui/widgets/matplotlib_canvas.py` — one shiboken `RuntimeError: FigureCanvasQTAgg already deleted` printed to stderr per full GUI run (repro: `test_source_instrument.py::TestReflectiveTab::test_solar_rows_do_not_open_an_editor`). **Not** the queued-idle-draw race CU-313 assumed: this matplotlib's `_draw_idle` already guards with `_isdeleted(self)`, and clearing `_draw_pending` at both discard and `destroyed` was measured not to silence it (2026-07-31 attempt, discarded as redundant). The traceback shows the C++ object valid at the `_isdeleted` check, gone by `self.update()` after the Agg render inside the same `_draw_idle` call — the deletion lands *mid-draw*, mechanism unidentified. matplotlib's own `except: print_exc` is what prints it; the draw is on a dying canvas so nothing is lost. Next attempt should start by finding what destroys the canvas during an in-progress synchronous draw. (origin: demoted from CU-313; sharpened 2026-07-31)
- 2026-07-31 `src/radiant/atmosphere/interpolated.py` — the shipped up-looking ladder is rendered at a single solar zenith (30°), so every query at any other `solar_zenith_rad` emits the CU-167 "is IGNORED" `UserWarning` twice per run, training operators to ignore it; de-duplicate per run, or retire when a multi-solar-zenith family ships (MODTRAN batch 2). (origin: demoted from CU-307)
- 2026-07-31 `src/radiant/geometry/stage.py` — a config with a target below its own terrain (`site_elevation_m = 900`, `target_altitude_m = 0`) passes GeometryStage silently and raises only inside `cn2()` when an HV profile is selected; a GeometryStage consistency check (both endpoints ≥ site elevation) would catch it at the right stage with the right framing (Rule 16). (origin: demoted from CU-303)
- 2026-07-31 `scenarios/01…1.3/outputs/` (fig2, fig3), `scenarios/03…3.2/outputs/` (3 figs), `scenarios/03…3.4/outputs/` (3 figs) — 8 committed figures are not byte-reproducible from their own unmodified generators (suspected hash-dependent series/legend ordering, same family as the fixed CU-292); diagnose the ordering, fix at source, regenerate once with cause. (origin: demoted from CU-291)
- 2026-07-31 `CLAUDE.md` (gate battery) — `scenarios/` sits outside both the lint and pytest gate scopes and nothing records whether that is a decision or an oversight (822 ruff findings there, all style, all parse clean); decide and write one sentence next to the gate battery — if it ever joins, it needs per-file-ignores for the narrative-script idiom first. (origin: demoted from CU-278)
- 2026-07-31 `src/radiant/gui/widgets/scripting_console.py:624` — every figure plotted from the scripting console is retained in `self._figure_windows` forever, even after the analyst closes its window, so hours-long sessions grow the live widget tree unbounded and the theme toggle re-polishes dead figures; decide closed-figure semantics (gone vs reopenable), then prune on close (`WA_DeleteOnClose` + `destroyed` hook) or cap the set. (origin: demoted from CU-285)
- 2026-07-31 `src/radiant/gui/widgets/spectral_integration_inputs_form.py` — `integration_time_s` is presented on the Spectral-Integration view under an *Acquisition* heading; relocate when a cross-stage acquisition grouping (TDI/binning timing) is ever designed. Presentation only, no schema change. (origin: demoted from CU-137)
- 2026-07-31 `src/radiant/gui/viewer/schematic_view.py::_arc_label_text` — the arc value label hardcodes `"°"` instead of sourcing the unit from the output key; provably correct today (the four-entry arc catalog is angle-only) — revisit only if a non-angle annotatable output is added (with CU-118 / Gap 87). (origin: demoted from CU-126)
- 2026-07-31 `src/radiant/geometry/stage.py` (no attitude output) — platform/sensor attitude (`observer_{yaw,pitch,roll}_rad`) has no stage owner; ADR-0006 §4's "GeometryStage output vs viewer-local input" decision stays open with no v1 consumer. Decide at the first task that actually needs sensor/platform attitude. (origin: demoted from CU-122 half (ii); half (i), the broken `dev_tools/geometry_gui_v2` shell, was already resolved by deletion 2026-07-19)
- 2026-08-01 `modtran/README.md` ("What's not here") — the NPZ-library coverage sentence still counts "25 of the 39 runs", batch-0 arithmetic that predates the 88-row (now 125-row) matrix; it is explicitly scoped to the archived plan §7.2 so it was left as-is when the rest of the README was refreshed for batch 2. Reword against the current matrix at the next README touch. (origin: P0 batch-2 deck authoring, branch modtran/batch2-decks)
- 2026-08-01 `docs/architecture/RADIANT_Atmosphere.md:797` — the three-way ANGLE-agreement claim says it covers "every delivered row of the 88-row matrix"; still true (the matrix is 125 rows but only 88 are delivered), and it goes stale the moment batch 2 (rows M1–Q8) is delivered. Re-verify and reword at batch-2 ingestion. (origin: P0 batch-2 deck authoring, branch modtran/batch2-decks)
- 2026-08-01 `scenarios/*/inputs/*.gui.yaml` (38 baselines) — no shipped golden or GUI baseline sets `atmosphere.model: interpolated` (even 8.1/8.2, the interpolation demonstrations, emit `model: simple`; their scripts use the separate `scripts/synth_modtran/family_interpolate.py` tool), so the whole interpolated backend has zero golden coverage — the results-affecting CU-306 change moved not one committed baseline. Consider one interpolated-backend baseline. (origin: CU-306 golden sweep)
- 2026-08-01 `scenarios/01…1.6/walkthrough.md`, `scenarios/10…10.2/walkthrough.md` — two more §5.3 refreshes that earlier results-affecting work skipped, found while refreshing the detection-range rows in the same tables: 1.6's signal/SNR were 24 345 e⁻ / 30.5 against the 21 229 e⁻ / 25.8 the runner produces today, and 10.2's sweep table was off in the fourth digit throughout (τ 0.6356 vs 0.6357, noise 735.5 vs 735.6 e⁻ RMS at 25 km). Both corrected in passing by the CU-263 PR with explicit attribution; the pattern — a results-affecting PR refreshing only the rows it moved — is what leaves these behind. (origin: CU-263 §5.3 sweep)
- 2026-08-01 `docs/architecture/RADIANT_Scripting_API.md:1334` — the "not a method" table still says "Detection range is not a computed metric; sweep `source.target.range_m` against an SNR threshold", which Gap 77 obsoleted in 2026-07: `detection_range_m` has been a `result.metrics` key since then. Reword to point at the metrics key like the RER/GSD half of the same row. (origin: CU-263 doc sweep)
- 2026-08-01 `scenarios/04…4.3/scripts/run_camouflage_analysis.py:43,150` — two pre-existing dead module-scope items surfaced by the CU-164 guard pass and deliberately left verbatim to keep source-line conservation checkable: `import warnings` is never used, and `IFOV_RAD` is computed but referenced nowhere (the pixel IFOV never enters the extended-regime construction). `ZENITH_MAX_RAD`/`BISECTION_STEPS` are also defined twice with identical values (lines 66-67 and again above `detection_range_km`). Harmless, invisible to the gate battery (`scenarios/` is outside the ruff scope), delete at the next real touch of this runner. (origin: CU-164 P7 close-out)
- 2026-08-01 `src/radiant/gui/viewer/viewer_state.py:145` — `ViewerState.theta_o_rad` defaults to `0.0` when the stage publishes no `theta_o_rad`, so the field's zero is ambiguous: it means both "vertical path" and "absent". Harmless while θ_o only drove an arc, but CU-250 made it place the sensor glyph, and the fallback there had to be written around the ambiguity (`theta_o_rad > 0.0 ? θ_o ray : η ray`, provably a no-op for a genuine vertical scene because a vertical path has η = 0 too). A `float | None` field, or deriving the fallback from `eta_rad` at the adapter, would remove the workaround. Every shipped geometry result publishes the key, so nothing is wrong today. (origin: CU-250, branch gui/p6a-301-250-110)
- 2026-08-01 `scripts/update_golden.py` — `RADIANT_Testing_Validation.md` §5.3 claims the update reason is appended to `_provenance.notes`, but the script has no `--reason` flag and never writes notes; every refresh hand-edits it (CU-267's did). Doc-claims-more-than-code gap; note the process-machinery moratorium before tooling it. (origin: CU-267)
- 2026-08-01 `docs/architecture/RADIANT_File_Tree.md` — the per-package file counts are a manual gate that any new test file trips (`check_org_rules` fails until the heading count is hand-edited); predictable friction on every test-adding PR. Inside the moratorium — recorded, no tooling change proposed. (origin: CU-267)
- 2026-08-01 `src/radiant/atmosphere/observer_leg.py:189` — the level arm's `tangent_depression_m` provenance prints `L²/(8 R_E)`, but the exact chord sag is `r_arm − √(r_arm² − (L/2)²)`, whose leading term is `L²/(8 r_arm)`. At a 10 km endpoint with a 150 km arm the printed value is 441.45 m against a true 440.78 m (0.15 % high); `level_arm.py`'s docstring and `LevelArmSpec` carry the same form. Provenance/doc only — no optical depth is computed from it (`level_whole_path.py` uses the exact chord). (origin: CU-224 P4, branch atmo/cu-224-unblocked-half)
- 2026-08-01 `src/radiant/gui/tests/test_configuration_manager.py:78` (and the `_open_study`/`_open_plain` twins in `test_configured_parameters.py`, `test_configuration_selector.py`) — after CU-289 removed the post-transaction evaluate-all waits, the remaining ~2 s/test in these three files is the *initial* pass every `_open_study` awaits (~90 windows × 2 s). It cannot be dropped the same way: `RADIANTMainWindow.__init__` starts the worker directly rather than through the debounce, so not waiting leaves a live `QThread` at teardown. Making the adopt-time evaluation debounced (or injecting a pre-computed first run) would take another ~60 s off the merge gate. (origin: CU-289, branch gui/p6b-289-239)
- 2026-08-01 `src/radiant/gui/tests/test_configured_parameters.py:103` — `_act(qtbot, window, action)` no longer uses its `qtbot` argument (CU-289 replaced the `waitSignal` body with a scheduled-state assert); kept for call-site symmetry with the other helpers rather than touching 20 call sites in a test-only commit. Drop the parameter at the next real touch of this file. (origin: CU-289, branch gui/p6b-289-239)
- 2026-08-01 `examples/mwir_leo_minimal.yaml:3,15` — the filename says LEO but the config is an 8 km airborne nadir case (`sensor_altitude_m: 8000.0`), which the header comment states plainly. Every GUI test opens this file, so a test that wants a down-looking space scene has to override the altitude first (the CU-239 picker tests do, with a comment explaining why an 8 km sensor under a 20 km target is an *up*-looking scene). Rename the example or the claim. (origin: CU-239, branch gui/p6b-289-239)
- 2026-08-02 `tests/integration/fixtures/modtran/` — the run matrix's `test_fixture + shipped_library` destination predates the owner's 2026-08-02 decision to track `modtran/real_runs/` in git (`c2587fd`). Copying the five promoted batch-2 tape7s into a committed fixture directory would now duplicate 21 MB of already-committed source data with two sources of truth (Rules 26/27), so the promotion shipped as a MANIFEST + anchor tests reading the canonical path instead. Either restate the matrix's `destination` vocabulary or record this as the convention. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)
- 2026-08-02 `tests/integration/test_modtran_manifest.py:26`, `tests/integration/test_uplooking_horizontal_anchors.py:60`, `tests/integration/test_modtran_real_runs.py` — the `skipif(not _REAL_RUNS.exists())` guards and their "gitignored until the fixture subset is committed — plan §7.1" reasons are stale since `c2587fd` tracked the run set. They now never skip, so nothing is wrong; the reason strings mislead a reader into thinking the data is absent in CI. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)
- 2026-08-02 `scripts/build_atmosphere_library.py` — the module still describes `modtran/real_runs/` as "staged, gitignored" in two places and `docs/architecture/RADIANT_Atmosphere.md` §3.2 said the same; corrected in the builder's generator line and the MANIFEST, but the phrase recurs across `modtran/README.md`, `scenarios/**/gaps.md`, and several test docstrings. One sweep when someone is next in those files. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)
- 2026-08-02 `docs/plans/modtran_run_matrix.csv` rows O1/O2 — marked `shipped_library`, but nadir-only: they have no zenith column, so they cannot join the rectangular `(sensor_altitude_m, path_zenith_rad)` upwelling grid the other three O rows form. Shipped under `validation/` (data, not interpolation nodes) instead. Adding them to `midlat_summer_sensor_ladder` would have re-baselined every existing off-node sensor-altitude query, which is out of scope for an ingestion. If a 1/5 km off-nadir upwelling family is wanted, two more decks (1 km and 5 km at 48.2°/60°) close the grid. (origin: P5b batch-2 ingestion, branch atmo/batch2-ingestion)
- 2026-08-02 `src/radiant/api/tests/test_config_io.py:150` — FIXED IN PASSING. `test_elements_change_results_physically` asserted `signal_e − nearfield_e` falls when a 0.97·0.96·0.90 element train replaces the example's `transmission_scalar: 0.70`. Both halves were wrong: `signal_e` is the scene integral alone (`nearfield_e` is published beside it and joins the well downstream), so the subtraction removed a term that was never added; and 0.83808 > 0.70, so the scene signal must *rise*. The inequality passed only because the spurious constant happened to outweigh the +19.7 % gain on the then-smaller signal, and CU-224's +53.5 % signal expired the coincidence. Replaced with a pin on the throughput ratio itself. (origin: CU-224 P5a §5.3 sweep, branch atmo/cu-224-lpath-thermal)
- 2026-08-02 `tests/integration/test_full_system.py::TestSweeps` — the two SNR-monotonicity sweeps ran into `_base_params`' 2e6 e- full well once CU-224 raised the 8 km MWIR scene's flux, flattening SNR at ≈ √FWC on the top rungs. Widened the well to 1e8 e- inside those two tests so the sweep stays in the photon-limited regime the assertion is about. The broader point: several shipped fixtures sit close enough to saturation that any results-affecting increase trips them, and the failure reads as a monotonicity violation rather than as "the well filled" — a `well_status` assertion in these trend tests would name it. (origin: CU-224 P5a §5.3 sweep, branch atmo/cu-224-lpath-thermal)
- 2026-08-02 `src/radiant/core/viewing_triangle.py` (via `atmosphere/observer_leg.py:193`) — `eta_from_theta_o(π, 0, h_tgt)` returns π exactly for h_tgt up to ~400 km but π − 8.9e-16 at a GEO target, so a vertical up-looking ray's sensor-side zenith `ζ_low` picks up 13 ULP of spherical-solve residue in an angle that is physically exactly zero. Nothing measurable moves (the companion's sky-at-aperture column carries it through at ~3e-15 relative), but it is why the CU-224 exo vacuum-identity test asserts the sky leg to `rel=1e-12` instead of bitwise, where the eight-field bundle is bitwise. (origin: CU-224 exo-guard lift, branch atmo/cu-224-lpath-thermal)
- 2026-08-02 `src/radiant/atmosphere/interpolation_coverage.py:507` — `check_interpolation_coverage`'s rule-2 remedy still names `recommended_axes(...)`, the axes-string-only heuristic. For an off-vertical up-looking scene it therefore says "Set atmosphere.interpolation_axes = 'target_altitude_m'", which selects the vertical-only ladder and is refused at evaluate — the same mis-recommendation CU-322 fixed on the `Sensor` seam. Not reachable as a wall any more (the GUI now shows the pre-validated gap advisory when nothing serves the scene, and the picker proposes the correct family beside the refusal when something does), but the message text is still one step behind the selector. Fixing it needs the LOS, which this module cannot build (Rule 11 forbids `atmosphere` importing `geometry`), so it is a design question, not a text edit. Candidate CU-322 checklist item. (origin: CU-322, branch gui/cu-322-interpolated-switch)
- 2026-08-02 `src/radiant/gui/widgets/atmosphere_family_picker.py:349` — the picker's coverage line quotes `ShippedFamily.coverage` verbatim for the pre-selected family even on a wholly-vacuum LEO→GEO path (scenario 10.4), where no backend is consulted at all: it reads "covers ground sensor (0 km) looking up at targets 0-20 km" beside a 500 km → GEO scene. The API now flags the case (`AtmosphereFamilySuggestion.vacuum_path`, used by the generated `gui_workflow.md` notes); the picker does not read it yet. (origin: CU-322, branch gui/cu-322-interpolated-switch)
- 2026-08-02 `scenarios/tools/gui_baselines.py` (CU-319 count) — the CU-319 entry names eight `AttributeError` baselines plus 4.3; a full `emit_gui_yaml.py` run on the unmodified base commit fails **thirteen**: 6.3 and 7.2 (`no attribute 'sensor'`) and 7.3 and 7.4 (`no attribute 'config'`) break the same way and were never listed. All thirteen are fixed in this PR (the CU-319 verification is 38/38 `[ok]`), so this is a record that the entry's enumeration was short, not open work. (origin: CU-319, branch fix/cu-319-318)
- 2026-08-02 `modtran/synthetic/` (`.gitignore:73`) vs `scenarios/08…8.2/scripts/` — scenario 8.2's runner dies with `FileNotFoundError: … modtran/synthetic/C5.synthetic.tp7` on any tree where `scripts/generate_synthetic_tape7.py` has not been run, because the synthetic decks are gitignored by design while `modtran/real_runs/` is now tracked. 8.2 is the only shipped runner with no real-run fallback, so it is the only one that cannot run from a fresh clone; scenario 1.1's walkthrough meanwhile advertises the synthetic set as the fallback that keeps scenarios "runnable from a bare clone", which is true only after the generator is run. Either commit the handful of decks the scenarios need, or say so in 8.2's docs. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `src/radiant/optics/` EE_box (CU-188 scope) — CU-188's CHANGELOG entry scopes the cell-area-overlap EE_box re-weighting to "point-source and sub-pixel" regimes, but the *reported* `EE(1x1)` metric moved by −14.2 % on EXTENDED scenes too (2.3, 6.3; also 8.3's vacuum rungs), where EE_box ≡ 1 and no signal changes. Harmless — only the diagnostic moved, not the radiometry — but the scope statement reads narrower than the change's actual reach, which made three scenario refreshes attribute it with a written caveat rather than cleanly. Widen the entry's scope wording. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `scenarios/06…6.4/scripts/run_synthetic_scene.py`, `scenarios/10…10.1/scripts/`, `10.3/scripts/`, `10.4/scripts/` — four runners print hardcoded narrative strings that their own computed output now contradicts: 6.4 prints "contrast SNR runs 59–548" while computing 49.3–475.5; 10.3 prints a "STILL ZERO" banner and an underflow explanation immediately after computing a sky background of 3.2774 W/m²/sr/µm, and still predicts the model is "~8× too opaque in the VIS" after its own check passes; 10.1 §6 attributes a detection-range crossover to a sky-thinning mechanism its own (now flat) table refutes; 10.4 §12 says the 180 K bench self-emission "is NOT zero and is left in the budget" while the chain warns it is identically zero. The walkthroughs were corrected by the CU-317 sweep, but these strings will re-stale the docs at the next refresh. Runner-side fix. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `tests/integration/test_uplooking_horizontal_anchors.py` — the pinned characterization values are a pre-CU-267 vintage and have drifted toward the loose end of their own tolerance bands (K-ladder MWIR τ ratios now 1.295/1.300/1.252/1.166/1.093 against pinned 1.299/1.306/1.259/1.173/1.100 in a ±0.04 band; level-arm LWIR 0.953/0.883/0.753 against 0.954/0.885/0.756 in ±0.03). Still green, but the margin is being spent silently, and the 10.2 walkthrough's "agrees to three figures" claim had to be softened because of it. Re-pin at the next touch. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `scenarios/*/gui_workflow.md` (2.5 Step 6, 6.2 lines 28–32, 5.2:59, 3.1) — four GUI mock-ups whose *design premise*, not merely their numbers, is falsified by current results: 2.5 specifies a stacked-bar panel around background_shot/signal_shot "swapping dominance" and a labelled BLIP region that no longer exists (signal_shot now runs 75.2 %→99.9 % of variance across the sweep); 6.2 states the now-reversed "transmittance residuals don't track SNR residuals" finding as a design requirement; 5.2:59's mock warning string says "8 µm fails MTF, EE, and SNR" though that candidate passes SNR and always did; 3.1 quotes NIIRS/GSD at 42° off-nadir, a point on no sweep grid. Redesign, not renumbering — deliberately left by the CU-317 sweep, which refreshed numbers only. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `scenarios/06…6.4/walkthrough.md` — the scenario's central methodological premise says filled-pixel target signals are range-independent, but its own runner shows S_tgt falling 8.853e5 → 6.352e5 e⁻ from 10 → 200 km as the atmospheric path grows. Equally false at the doc's own vintage, so it is not sweep drift; the CU-317 pass declined to rewrite a scenario's core narrative claim unilaterally. Needs a physics ruling, then a rewrite. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `scenarios/10…10.3/scripts/`, block 10 `default_off_metrics` — two small unattributables found by the sweep: 10.3 raises two full-well saturation `UserWarning`s (4.627e5 e⁻ against a 4e5 e⁻ well) from its GEO daylight reflective probe that the runner never surfaces in stdout, and `diffraction_limit_target_plane_m` joined the scene-class `default_off_metrics` list for all four block-10 scenarios (10 → 11 entries) with no Results-affecting CHANGELOG entry covering it. Neither changes a reported number; both were refreshed in the docs as observed rather than attributed. (origin: CU-317 scenario-walkthrough sweep)
- 2026-08-02 `examples/scripts/compare_configs.py` — `examples/` is outside the four trees the ruff gate governs (`src/ tests/ scripts/ dev_tools/`, widened by CU-252/CU-270), and `ruff format --check examples/` already reports this file as drifted. Shipped example scripts are read as reference code and are edited by tasks that touch physics (this one was), so the same drift the two earlier widenings closed is accumulating in a fifth tree. One-line gate question, not a defect. (origin: CU-315 example-caveat update, branch fix/cu-315-323)
- 2026-08-02 `tests/integration/test_modtran_tape7_import.py`, `test_modtran_real_runs.py`, `test_segment_modtran_anchors.py`, `test_batch2_fixture_anchors.py` — no committed integration anchor exercises a tabulated- or MODTRAN-served τ on a chain grid that differs from the file's: every one of them either reads the parser output directly (`_read`/`_components`), queries on the file's own grid (a bit-identical no-op), or anchors a radiance/irradiance (which stays linear). CU-316 changed the τ resample convention for both backends — a results-affecting change — and the §5.3 sweep therefore moved **zero** committed anchors; the movement is pinned only by the new Level-0 tests. Same class of gap the CU-306 closure logged for `atmosphere.model: interpolated`. One off-node-grid anchor per backend would close it. (origin: CU-316, branch atmo/cu-316-logtau-backends)
- 2026-08-02 `src/radiant/atmosphere/interpolated.py:968` vs `log_tau_resample.py` — the two log-τ paths disagree on over-unity τ: the interpolated backend clips its result to [0, 1] after `exp()` (and its constructor clips stored τ to [TAU_FLOOR, 1.0] before the log), so a mis-scaled family file is silently snapped to a plausible column, while the shared `resample_transmittance` floors only at TAU_FLOOR so the same defect in a tabulated/MODTRAN file still fails loud in `AtmosphericQuantities.__post_init__` (Rule 17). CU-316 deliberately did not touch the interpolated behaviour; the asymmetry is now the only place the three backends' τ conventions differ. (origin: CU-316, branch atmo/cu-316-logtau-backends)
- 2026-08-02 `scenarios/01…1.1/walkthrough.md`, `scenarios/06…6.2/walkthrough.md` — CU-316's log-τ resample DID move committed scenario prose, contradicting its own closure note that "zero committed goldens/baselines move": 1.1's MODTRAN-6 column reads τ 0.4319 → 0.4277 / SNR 917.36 → 916.18 / range 2239.7 → 2227.3 km, and 6.2's SNR-MODTRAN column moves 0.5–0.7 % on every profile. Both are tape7-import arms, which is exactly the backend CU-316 changed. The closure's claim was true of machine baselines and goldens (none select those backends) and false of runner-generated walkthrough tables, which no §5.3 sweep covers by default — the CU-317 class of gap, one week later. Refreshed in passing by the CU-321 sweep with CU-316 attribution. (origin: CU-321, branch atmo/cu-321-height-teff)
- 2026-08-03 `src/radiant/atmosphere/tests/test_near_horizon_handover.py` (module docstring + `test_the_step_at_the_switch_is_bounded_and_downward`) — the prose says "the optical depth drops by ≈ 3.6 %" but 3.62 % is the molecular AIR-MASS drop; the median OD drops 2.0 % and median τ rises 10.6 % (measured live during the P-D1 extraction; the assertion band 0.960–1.0 is correct and passes on 0.9802). Docstring-only looseness — tighten at the next touch. (origin: Atmosphere Documentation Plan P-D1)
- 2026-08-03 `src/radiant/atmosphere/` (θ_s-stripped `T1Thermal` sky coupling) — the limitation `RADIANT_Atmosphere.md` §4.2g records as "found 2026-07-26, not yet repaired" has NO registry entry anywhere — only a characterization test and the doc note; it predates the two-tier intake. Now also item 12 of `docs/validation/atmosphere_modtran_parity.md`'s limitations register. Decide its tier at next triage. (origin: Atmosphere Documentation Plan P-D1)
- 2026-08-03 `src/radiant/api/atmosphere_families.py:73-75` — `shipped_atmosphere_families()` docstring says `midlat_summer_boost_ladder` is the only `explicit_dir_only` row; `midlat_summer_sst_column_fan` became the second at CU-322 intake. Stale docstring — fix at the next touch of that module. (origin: Atmosphere Documentation Plan P-D2)
- 2026-08-03 `src/radiant/gui/stage_views.py` / metric registry — "Diffraction limit (at target, legacy key)" renders as a visible duplicate row; "Sampling regime 0 code" and "NIIRS (extrapolated) 1 0/1 flag" leak raw registry vocabulary to the analyst-facing default screen. Decode table or suppression at display time. (origin: GUI design critique, metric cards)
- 2026-08-03 `docs/architecture/RADIANT_Atmosphere.md` §3.1 + §4.2g — two measured records still live in the slimmed architecture doc because `atmosphere_modtran_parity.md` has no corresponding table: the A2–A6 profile cross-validation claim (≤ ±0.012 band-mean τ) and the CU-254 `background_e` measurement (1.94207e5/2.14046e5/2.21479e5 e⁻). Re-home both into the parity doc at its next touch, then trim here. (origin: Atmosphere Documentation Plan P-D3)
- 2026-08-03 `mkdocs.yml` — the new Validation nav section navigates only `atmosphere_modtran_parity.md`; the five pre-existing `docs/validation/` documents were never in the nav and remain out. Decide whether they join at the next docs-infrastructure touch. (origin: Atmosphere Documentation Plan P-D3)
- 2026-08-03 `src/radiant/atmosphere/family_suitability.py:715` — with two SST fans rendered at different lower endpoints, a scene at a third elevation reports whichever fan comes first in `BUNDLED_FAMILIES` (the 0 m one), not the nearer one: `select_atmosphere_family` breaks equal-gate ties by catalogue order, never by how close the miss is. Advisory quality only, and no shipped scenario sits at a third elevation. (origin: M9-M13 / P7-P8 follow-on ingestion)
- 2026-08-03 `src/radiant/api/atmosphere_families.py:73-75` — the stale-docstring line already logged 2026-08-03 is now two families out of date: `EXPLICIT_DIR_FAMILIES` holds three rows since the 900 m SST fan shipped. Fold into that line's fix. (origin: M9-M13 / P7-P8 follow-on ingestion)
- 2026-08-03 (process) — the batch-4 merge (`474cd0c8`) briefly took `main` red on one GUI test: the battery invocation piped `pytest -q` through `tail`, so the pipeline's exit status was tail's and the failure was masked until after the push. Fixed forward within minutes (`49b9fc17` — the test carried a pre-CU-329 expectation; the merged code was correct). Agent practice, not tooling: battery invocations must `set -o pipefail` or capture pytest's status directly before merging. (origin: gui/param-panel-finish merge)
