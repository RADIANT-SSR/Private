# Scenario 2.8 — issues found

None new. Two known limitations exercised knowingly:

- The GeoSnap-18 preset ships no dark current (cutoff-dependent; preset notes
  explain) — the scenario supplies an explicit programme estimate, which is
  the intended workflow, not a gap.
- NEDT is not reported for this configuration (metric-selection dependent);
  the scenario's headline metric is SNR.
