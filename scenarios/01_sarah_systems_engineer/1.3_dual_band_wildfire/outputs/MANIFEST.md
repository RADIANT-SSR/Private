# Output Manifest

All artifacts below are produced by `scripts/run_dual_band_trade.py` from
`inputs/forest_conifer_aster.txt` + `inputs/sarah_detector_options.xlsx`
(both generated by `inputs/create_spreadsheet.py`). Regenerate by running
the script from the repo root. Committed figures are referenced by
`walkthrough.md`; `*_results.xlsx` workbooks are regenerate-on-demand and
not committed (see `docs/OPERATING_MODEL.md` Rule 26).

| Artifact | Kind | Last generating commit |
|---|---|---|
| fig1_spectral_contrast.png | figure (committed) | (set on commit) |
| fig2_detection_vs_temperature.png | figure (committed) | (set on commit) |
| fig3_noise_budgets.png | figure (committed) | (set on commit) |
| dual_band_results.xlsx | results workbook (not committed) | regenerate on demand |
