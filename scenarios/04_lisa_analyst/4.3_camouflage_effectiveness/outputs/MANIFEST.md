# Output Manifest

All artifacts below are produced by `scripts/run_camouflage_analysis.py` from
`inputs/steel_oxidized_aster.txt`, `inputs/camo_net_{a,b,c}.csv`, and
`inputs/lisa_flir_sensor.xlsx` (all generated by `inputs/create_spreadsheet.py`).
Regenerate by running the script from the repo root. Committed figures are
referenced by `walkthrough.md`; `outputs/derived/*.csv` (the S8 radiance inputs)
and `*_results.xlsx` are regenerate-on-demand and not committed
(see `docs/OPERATING_MODEL.md` Rule 26).

| Artifact | Kind | Last generating commit |
|---|---|---|
| fig1_spectral_contrast.png | figure (committed) | (set on commit) |
| fig2_signature_by_option.png | figure (committed) | (set on commit) |
| fig3_emissivity_inputs.png | figure (committed) | (set on commit) |
| camouflage_results.xlsx | results workbook (not committed) | regenerate on demand |
| derived/*.csv | S8 radiance inputs (not committed) | regenerate on demand |
