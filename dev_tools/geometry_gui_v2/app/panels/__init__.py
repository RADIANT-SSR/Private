"""Qt UI panels — collapsible right-dock sections.

Per Rule 19 / C5, each panel lives in its own module. Panels here may
import from PySide6; this is the UI shell layer (the only allowed
direction per C7: shell → scene library, never the reverse).
"""
