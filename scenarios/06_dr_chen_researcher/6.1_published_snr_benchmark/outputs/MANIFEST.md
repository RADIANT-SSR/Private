# Output Manifest

All artifacts below are produced by `scripts/run_datasheet_benchmark.py`
from `inputs/chen_lwir_datasheet.xlsx` (generated by
`inputs/create_spreadsheet.py`). Regenerate by running the script from the
repo root. The committed figure is referenced by `walkthrough.md`; the
benchmark table is printed to the console.

| Artifact | Kind | Last generating commit |
|---|---|---|
| fig1_datasheet_benchmark.png | figure (committed) | 55b0175 2026-07-08 |
