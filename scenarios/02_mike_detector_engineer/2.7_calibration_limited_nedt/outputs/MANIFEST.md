# Outputs Manifest — Scenario 2.7

| File | Generator | Input |
|---|---|---|
| `calibration_limited_nedt_results.csv` | `scripts/run_calibration_limited_nedt.py` | `inputs/mike_calibration_specs.yaml` |

Regenerate with `python scripts/run_calibration_limited_nedt.py` from the
scenario's `scripts/` directory. Committed because `walkthrough.md` cites its
numbers (Rule 26).
