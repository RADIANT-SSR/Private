# Output Manifest — Scenario 10.2

Every artifact below is produced by
`scripts/run_air_to_air_level_irst.py`, which reads
`inputs/irst_air_to_air_vendor_data.xlsx` (generated by
`inputs/create_spreadsheet.py`) and, for the cross-model anchor, the delivered
MODTRAN 6 horizontal runs `L16`–`L20` from `modtran/real_runs/` (gitignored
staging set — see `modtran/real_runs/README.md`).

Regenerate from the repo root:

```bash
python scenarios/10_direction_general/10.2_air_to_air_level_irst/inputs/create_spreadsheet.py
python scenarios/10_direction_general/10.2_air_to_air_level_irst/scripts/run_air_to_air_level_irst.py
```

If the MODTRAN runs are not staged, the runner prints a SKIPPED banner naming
the missing runs and figure 2 loses its MODTRAN markers; every other artifact is
unaffected. **The committed figures are from the real staged runs.**

| Artifact | Kind | Data source | Generating commit |
|---|---|---|---|
| `10.2_snr_and_detection_range_vs_range.png` | figure (committed) | chain sweep, simple atmosphere | see `git log -1 -- <file>` |
| `10.2_transmittance_vs_modtran_lgrid.png` | figure (committed) | chain sweep + real MODTRAN 6 L16–L20 | see `git log -1 -- <file>` |
| `10.2_horizon_guard_tangent_depression.png` | figure (committed) | `core.viewing_triangle.classify_horizon_topology` over the sweep | see `git log -1 -- <file>` |
| `10.2_los_rate_and_smear_budget.png` | figure (committed) | chain sweep, K0 and K2 kinematics doors | see `git log -1 -- <file>` |
| `10.2_air_to_air_results.xlsx` | workbook (**not** committed, gitignored per Rule 26) | chain sweep + MODTRAN anchor | regenerate on demand |

All four figures are referenced by `walkthrough.md`, which is what qualifies
them for commit under Rule 26(b).
