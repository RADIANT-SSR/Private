"""PySide6 application shell — window, docks, menu bar, settings.

The app shell depends on :mod:`dev_tools.geometry_gui_v2.scene`, never the
reverse (constraint C7). The ``scene`` package must remain importable from
a Jupyter notebook with only PyVista installed.
"""
